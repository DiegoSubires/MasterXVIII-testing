This file is a merged representation of a subset of the codebase, containing specifically included files, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: src/**/*
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
src/
  assets/
    favicon.ico
  common/
    components/
      confirmation-dialog/
        confirmation-dialog.component.tsx
        confirmation-dialog.hook.ts
        index.ts
      dashboard/
        components/
          index.ts
          item.component.spec.tsx
          item.component.tsx
          item.styles.ts
        dashboard.component.spec.tsx
        dashboard.component.tsx
        dashboard.styles.ts
        dashboard.vm.ts
        index.ts
      footer/
        components/
          copyright.component.tsx
          index.ts
        footer.component.tsx
        footer.styles.ts
        index.ts
      form/
        checkbox/
          checkbox.component.tsx
          checkbox.styles.ts
          index.ts
        select/
          index.ts
          select.component.tsx
          select.styles.ts
        form.component.tsx
        index.ts
        text-field.component.tsx
      pagination/
        index.ts
        pagination.component.tsx
      search-bar/
        index.ts
        search-bar.component.tsx
        search-bar.hook.ts
      snackbar/
        index.ts
        snackbar.component.tsx
        snackbar.context.tsx
        snackbar.hook.tsx
        snackbar.styles.ts
        snackbar.vm.ts
      spinner/
        index.ts
        spinner.component.tsx
        spinner.styles.ts
      table/
        components/
          body.component.spec.tsx
          body.component.tsx
          cell.component.spec.tsx
          cell.component.tsx
          cell.styles.ts
          header.component.spec.tsx
          header.component.tsx
          index.ts
          row.component.spec.tsx
          row.component.styles.ts
          row.component.tsx
        index.ts
        table.component.tsx
        table.container.tsx
        table.mappers.ts
        table.styles.ts
        table.vm.ts
      tabs/
        index.ts
        tab-list.component.tsx
        tab-panel.component.tsx
        tab.component.tsx
      index.ts
    constants/
      index.ts
      months.ts
    helpers/
      filter.helpers.spec.ts
      filter.helpers.ts
      index.ts
      is-edit-mode.helper.spec.ts
      is-edit-mode.helper.ts
      object.helpers.spec.ts
      object.helpers.ts
    hooks/
      debounce.hook.ts
      index.ts
    mappers/
      collection.mapper.spec.ts
      collection.mapper.ts
      index.ts
    models/
      index.ts
      lookup.ts
    test/
      index.ts
      render-with-router.tsx
  common-app/
    app-bar/
      components/
        index.ts
        side-list.component.tsx
        side-list.styles.ts
        side-menu.component.tsx
      app-bar.component.tsx
      app-bar.hook.ts
      app-bar.styles.ts
      index.ts
    auth/
      auth-route.component.tsx
      auth.context.tsx
      auth.hooks.ts
      auth.vm.ts
      index.ts
    command-footer/
      translations/
        en.ts
        es.ts
        index.ts
        keys.ts
      command-footer.component.tsx
      command-footer.style.ts
      index.ts
  core/
    router/
      index.ts
      router.component.tsx
      routes.ts
    theme/
      index.ts
      theme-provider.component.tsx
      theme.ts
    i18n.ts
  layouts/
    app.layout.styles.ts
    app.layout.tsx
    centered.layout.styles.ts
    centered.layout.tsx
    index.ts
  pods/
    employee/
      api/
        employee.api-model.ts
        employee.api.ts
        employee.mock-data.ts
        index.ts
      components/
        data.component.tsx
        data.styles.ts
        data.validations.ts
        index.ts
        project-row.component.tsx
        project.component.tsx
        report.component.tsx
        report.styles.ts
        report.validations.ts
      employee.component.tsx
      employee.container.tsx
      employee.mappers.spec.ts
      employee.mappers.ts
      employee.styles.ts
      employee.vm.ts
      index.ts
    employee-list/
      api/
        employee-list.api-model.ts
        employee-list.api.ts
        employee-list.mock-data.ts
        index.ts
      components/
        employee-row.component.tsx
        index.ts
      employee-list.component.tsx
      employee-list.container.tsx
      employee-list.mappers.spec.ts
      employee-list.mappers.ts
      employee-list.vm.ts
      index.ts
    login/
      components/
        container.component.tsx
        container.styles.ts
        index.ts
        login-form.component.tsx
        login-form.styles.ts
      index.ts
      login.api.ts
      login.component.tsx
      login.container.tsx
      login.mapper.ts
      login.validation.ts
      login.vm.ts
    project/
      api/
        employee.api.ts
        index.ts
        project.api-model.ts
        project.mock-data.ts
      components/
        data.component.tsx
        employee-row.component.tsx
        employee.component.tsx
        index.ts
        report.component.tsx
        report.styles.ts
      index.ts
      project.component.tsx
      project.container.tsx
      project.mapper.ts
      project.styles.ts
      project.vm.ts
    project-list/
      api/
        index.ts
        project-list.api-model.ts
        project-list.api.ts
        project-list.mock-data.ts
      components/
        index.ts
        project-row.component.tsx
      index.ts
      project-list.component.tsx
      project-list.container.tsx
      project-list.mappers.spec.ts
      project-list.mappers.ts
      project-list.vm.ts
    submodule-list/
      index.ts
      submodule-list.component.tsx
      submodule-list.container.tsx
  scenes/
    employee-list.scene.tsx
    employee.scene.tsx
    index.ts
    login.scene.tsx
    project-list.scene.tsx
    project.scene.tsx
    submodule-list.scene.tsx
  app.tsx
  index.tsx
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/common/components/confirmation-dialog/confirmation-dialog.component.tsx">
import React from 'react';
import {
  Dialog,
  DialogTitle,
  DialogContent,
  DialogActions,
  Button,
} from '@mui/material';

interface LabelProps {
  closeButton: string;
  acceptButton: string;
}

interface Props {
  isOpen: boolean;
  onAccept: () => void;
  onClose: () => void;
  title: string | React.ReactNode;
  labels: LabelProps;
  children: React.ReactNode;
}

export const ConfirmationDialogComponent: React.FunctionComponent<Props> = (
  props
) => {
  const { isOpen, onAccept, onClose, title, labels, children } = props;

  const handleAccept = () => {
    onAccept();
    onClose();
  };

  return (
    <Dialog open={isOpen}>
      <DialogTitle>{title}</DialogTitle>
      <DialogContent>{children}</DialogContent>
      <DialogActions>
        <Button onClick={onClose} color="secondary" variant="contained">
          {labels.closeButton}
        </Button>
        <Button onClick={handleAccept} color="primary" variant="contained">
          {labels.acceptButton}
        </Button>
      </DialogActions>
    </Dialog>
  );
};
</file>

<file path="src/common/components/confirmation-dialog/confirmation-dialog.hook.ts">
import React from 'react';
import { createEmptyLookup, Lookup } from '#common/models';

export const useConfirmationDialog = () => {
  const [isOpen, setIsOpen] = React.useState(false);
  const [itemToDelete, setItemToDelete] = React.useState(createEmptyLookup());

  return {
    isOpen,
    itemToDelete,
    onAccept: () => {
      setItemToDelete(createEmptyLookup());
    },
    onClose: () => setIsOpen(false),
    onOpenDialog: (item: Lookup) => {
      setIsOpen(true);
      setItemToDelete(item);
    },
  };
};
</file>

<file path="src/common/components/confirmation-dialog/index.ts">
export * from './confirmation-dialog.component';
export * from './confirmation-dialog.hook';
</file>

<file path="src/common/components/dashboard/components/index.ts">
export * from './item.component';
</file>

<file path="src/common/components/dashboard/components/item.component.spec.tsx">
import React from 'react';
import PeopleAltIcon from '@mui/icons-material/PeopleAlt';
import { fireEvent } from '@testing-library/react';
import { renderWithRouter } from '#common/test';
import { Route } from 'react-router-dom';
import { ItemComponent, ClassesProps } from './item.component';
import { DashboardItemProps } from '../dashboard.vm';

describe('common/dashboard/ItemComponent', () => {
  it('should be render as expected passing required properties', () => {
    // Arrange
    const props = {
      item: {
        icon: PeopleAltIcon,
        title: 'test name',
        linkTo: '/test-link',
      } as DashboardItemProps,
      dataTestId: 'test-item',
    };

    // Act
    const { getByText } = renderWithRouter(
      <ItemComponent {...props} />,
      <>
        <Route
          path={props.item.linkTo}
          element={<h1>Test route destination</h1>}
        />
      </>
    );

    // Assert
    expect(getByText(props.item.title)).toBeInTheDocument();
  });

  it('should be render as expected passing required and optional properties', () => {
    // Arrange
    const props = {
      item: {
        icon: PeopleAltIcon,
        title: 'test name',
        linkTo: '/test-link',
        subtitle: 'test subtitle',
      } as DashboardItemProps,
      classes: {
        root: 'test-root-class',
        icon: 'test-icon-class',
        title: 'test-name-class',
        subtitle: 'test-subtitle-class',
      } as ClassesProps,
      dataTestId: 'test-item',
    };

    // Act
    const { getByTestId, getByText } = renderWithRouter(
      <ItemComponent {...props} />,
      <>
        <Route
          path={props.item.linkTo}
          element={<h1>Test route destination</h1>}
        />
      </>
    );

    const element = getByTestId(props.dataTestId);

    // Assert
    expect(element).toHaveClass(props.classes.root);
    expect(getByText(props.item.title)).toHaveClass(props.classes.title);
    expect(getByText(props.item.subtitle)).toHaveClass(props.classes.subtitle);
  });

  it('should navigate to route when click on item component', () => {
    // Arrange
    const props = {
      item: {
        icon: PeopleAltIcon,
        title: 'test name',
        linkTo: '/test-link',
        subtitle: 'test subtitle',
      } as DashboardItemProps,
      classes: {
        root: 'test-root-class',
        icon: 'test-icon-class',
        title: 'test-name-class',
        subtitle: 'test-subtitle-class',
      } as ClassesProps,
      dataTestId: 'test-item',
    };

    // Act
    const { getByTestId, getByText } = renderWithRouter(
      <ItemComponent {...props} />,
      <>
        <Route
          path={props.item.linkTo}
          element={<h1>Test route destination</h1>}
        />
      </>
    );

    const element = getByTestId(props.dataTestId);
    fireEvent.click(element);

    // Assert

    expect(getByText('Test route destination')).toBeInTheDocument();
  });
});
</file>

<file path="src/common/components/dashboard/components/item.component.tsx">
import React from 'react';
import { cx } from '@emotion/css';
import { Typography } from '@mui/material';
import { Link } from 'react-router-dom';
import { DashboardItemProps } from '../dashboard.vm';
import * as innerClasses from './item.styles';

export interface ClassesProps {
  root?: string;
  icon?: string;
  title?: string;
  subtitle?: string;
}

interface Props {
  item: DashboardItemProps;
  classes?: ClassesProps;
  dataTestId?: string;
}

export const ItemComponent: React.FC<Props> = (props) => {
  const {
    item: { icon: Icon, title, linkTo, subtitle },
    classes = {
      root: '',
      icon: '',
      title: '',
      subtitle: '',
    },
    dataTestId,
  } = props;
  return (
    <Link
      className={cx(innerClasses.root, classes.root)}
      to={linkTo}
      data-testid={dataTestId}
    >
      <Icon className={cx(innerClasses.icon, classes.icon)} />
      <Typography
        variant="h5"
        className={cx(innerClasses.title, classes.title)}
      >
        {title}
      </Typography>
      <Typography
        variant="h6"
        className={cx(innerClasses.subtitle, classes.subtitle)}
      >
        {subtitle}
      </Typography>
    </Link>
  );
};
</file>

<file path="src/common/components/dashboard/components/item.styles.ts">
import { css } from '@emotion/css';

export const root = css`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 50px;
  border: none;
  text-decoration: none;
  color: #333;

  &:hover {
    background-color: #e6e6e6;
  }
`;

export const icon = css`
  font-size: 5rem;
`;

export const title = css`
  margin-top: 20px;
`;

export const subtitle = css`
  margin-top: 20px;
`;
</file>

<file path="src/common/components/dashboard/dashboard.component.spec.tsx">
import * as React from 'react';
import PeopleAltIcon from '@mui/icons-material/PeopleAlt';
import { Route } from 'react-router-dom';
import { renderWithRouter } from '#common/test';
import { DashboardComponent } from './dashboard.component';
import { DashboardItemProps } from './dashboard.vm';

describe('common/DashboardComponent', () => {
  it('should be render as expected passing required properties', () => {
    // Arrange
    const props = {
      items: [
        {
          title: 'test name',
          icon: PeopleAltIcon,
          linkTo: 'linkTo',
        },
      ] as DashboardItemProps[],
    };

    // Act
    const { getByText } = renderWithRouter(
      <DashboardComponent {...props} />,
      <>
        <Route
          path={props.items[0].linkTo}
          element={<h1>Test route destination</h1>}
        />
      </>
    );

    // Assert
    expect(getByText(props.items[0].title)).toBeInTheDocument();
  });

  it('should be render as expected passing required and optional properties', () => {
    // Arrange
    const props = {
      items: [
        {
          title: 'test name',
          icon: PeopleAltIcon,
          linkTo: 'linkTo',
        },
      ] as DashboardItemProps[],
      classes: {
        root: 'test-root-class',
        items: 'test-items-class',
        item: {
          root: 'test-item-class',
        },
      },
      dataTestId: 'dashboard-id',
    };

    // Act
    const { getByTestId } = renderWithRouter(
      <DashboardComponent {...props} />,
      <>
        <Route
          path={props.items[0].linkTo}
          element={<h1>Test route destination</h1>}
        />
      </>
    );

    const element = getByTestId(props.dataTestId);

    // Assert
    expect(element).toHaveClass(props.classes.root);
    expect(element.firstChild).toHaveClass(props.classes.items);
  });

  it('should be render as expected passing three items', () => {
    // Arrange
    const props = {
      items: [
        {
          title: 'test name 1',
          icon: PeopleAltIcon,
          linkTo: 'linkTo 1',
        },
        {
          title: 'test name 2',
          icon: PeopleAltIcon,
          linkTo: 'linkTo 2',
        },
        {
          title: 'test name 3',
          icon: PeopleAltIcon,
          linkTo: 'linkTo 3',
        },
      ] as DashboardItemProps[],
      classes: {
        dashboard: 'test-dashboard-class',
        items: 'test-items-class',
        item: {
          item: 'test-item-class',
          icon: 'test-icon-class',
          name: 'test-name-class',
        },
      },
      dataTestId: 'dashboard-id',
    };

    // Act
    const { getByText } = renderWithRouter(
      <DashboardComponent {...props} />,
      <>
        <Route path={props.items[0].linkTo} element={<h1>Test route 1</h1>} />
        <Route path={props.items[1].linkTo} element={<h1>Test route 2</h1>} />
        <Route path={props.items[2].linkTo} element={<h1>Test route 3</h1>} />
      </>
    );

    // Assert
    expect(getByText(props.items[0].title)).toBeInTheDocument();
    expect(getByText(props.items[1].title)).toBeInTheDocument();
    expect(getByText(props.items[2].title)).toBeInTheDocument();
  });
});
</file>

<file path="src/common/components/dashboard/dashboard.component.tsx">
import React from 'react';
import { cx } from '@emotion/css';
import { ItemComponent, ClassesProps } from './components';
import { DashboardItemProps } from './dashboard.vm';
import * as innerClasses from './dashboard.styles';

interface ClassNameProps {
  root?: string;
  items?: string;
  item?: ClassesProps;
}

interface Props {
  items: DashboardItemProps[];
  classes?: ClassNameProps;
  dataTestId?: string;
}

export const DashboardComponent: React.FC<Props> = (props) => {
  const {
    items,
    classes = {
      root: '',
      items: '',
      item: {
        root: '',
        icon: '',
        title: '',
      },
    },
    dataTestId,
  } = props;
  return (
    <div
      data-testid={dataTestId}
      className={cx(innerClasses.root, classes.root)}
    >
      <div className={cx(innerClasses.items, classes.items)}>
        {items.map(
          (item) =>
            Boolean(item) && (
              <ItemComponent
                key={item.title}
                classes={{
                  ...classes.item,
                  root: cx(innerClasses.item, classes.item.root),
                }}
                item={item}
              />
            )
        )}
      </div>
    </div>
  );
};
</file>

<file path="src/common/components/dashboard/dashboard.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const root = css`
  display: flex;
  flex-direction: column;
`;

export const items = css`
  display: grid;
  grid-template-columns: repeat(1, 1fr);
  grid-gap: 4rem;

  @media (min-width: ${theme.breakpoints.values.sm}px) {
    grid-template-columns: repeat(3, 1fr);
  }
`;

export const item = css`
  padding: 2rem 0rem;
`;
</file>

<file path="src/common/components/dashboard/dashboard.vm.ts">
export interface DashboardItemProps {
  title: string;
  linkTo: string;
  icon?: React.ComponentType<{ className: string }>;
  image?: string;
  subtitle?: string;
}
</file>

<file path="src/common/components/dashboard/index.ts">
export * from './dashboard.component';
export * from './dashboard.vm';
</file>

<file path="src/common/components/footer/components/copyright.component.tsx">
import React from 'react';
import { Typography, Link } from '@mui/material';
import { literals } from '#core/i18n';

export const CopyrightComponent: React.FC = () => (
  <Typography variant="body2" align="center">
    {' © '}
    <Link
      color="inherit"
      target="_blank"
      rel="noopener"
      href="https://lemoncode.net/"
    >
      {literals.global.lemoncode}
    </Link>{' '}
    {new Date().getFullYear()}
    {` - ${literals.global.copyrightFooter}`}
  </Typography>
);
</file>

<file path="src/common/components/footer/components/index.ts">
export * from './copyright.component';
</file>

<file path="src/common/components/footer/footer.component.tsx">
import React from 'react';
import { Typography } from '@mui/material';
import { CopyrightComponent } from './components';
import * as classes from './footer.styles';

export const FooterComponent: React.FC = () => (
  <footer className={classes.footer}>
    <Typography variant="h6" align="center" gutterBottom>
      Origin
    </Typography>
    <CopyrightComponent />
  </footer>
);
</file>

<file path="src/common/components/footer/footer.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const footer = css`
  background-color: ${theme.palette.primary.light};
  padding: ${theme.spacing(2)};
  color: ${theme.palette.common.white};
`;
</file>

<file path="src/common/components/footer/index.ts">
export * from './footer.component';
</file>

<file path="src/common/components/form/checkbox/checkbox.component.tsx">
import React from 'react';
import { useField } from 'formik';
import {
  Checkbox,
  CheckboxProps,
  FormControlLabel,
  FormControl,
  FormHelperText,
} from '@mui/material';
import * as classes from './checkbox.styles';

interface Props extends CheckboxProps {
  label?: string;
  labelPlacement?: 'end' | 'start' | 'top' | 'bottom';
  error?: boolean;
  helperText?: string;
}

export const CheckboxComponent: React.FunctionComponent<Props> = (props) => {
  const {
    label,
    labelPlacement,
    error,
    name,
    checked,
    onChange,
    onBlur,
    ...checkboxProps
  } = props;
  const [field, meta] = Boolean(name) ? useField(name) : [];
  const hasError = error || Boolean(meta && meta.touched && meta.error);
  const helperText = Boolean(field) ? meta?.error : props.helperText;

  return (
    <FormControl
      className={classes.root}
      error={hasError}
      fullWidth={true}
      margin="normal"
    >
      <FormControlLabel
        control={
          <Checkbox
            {...checkboxProps}
            name={name}
            checked={checked || field?.value}
            onChange={onChange || field?.onChange}
            onBlur={onBlur || field?.onBlur}
          />
        }
        label={label}
        labelPlacement={labelPlacement}
      />

      {hasError && <FormHelperText>{helperText}</FormHelperText>}
    </FormControl>
  );
};
</file>

<file path="src/common/components/form/checkbox/checkbox.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const root = css`
  justify-content: flex-end;
`;
</file>

<file path="src/common/components/form/checkbox/index.ts">
export * from './checkbox.component';
</file>

<file path="src/common/components/form/select/index.ts">
export * from './select.component';
</file>

<file path="src/common/components/form/select/select.component.tsx">
import React from 'react';
import { useField } from 'formik';
import {
  FormControl,
  InputLabel,
  Select,
  SelectProps,
  MenuItem,
  FormHelperText,
} from '@mui/material';
import { Lookup } from '#common/models';
import * as classes from './select.styles';

type Props = SelectProps<any> & {
  items: Lookup[];
  helperText?: string;
};

export const SelectComponent: React.FunctionComponent<Props> = (props) => {
  const {
    name,
    items,
    label,
    error,
    onChange,
    onBlur,
    className,
    ...otherProps
  } = props;
  const [field, meta] = Boolean(name) ? useField(name) : [];
  const hasError = error || Boolean(meta && meta.touched && meta.error);
  const helperText = Boolean(field) ? meta?.error : props.helperText;
  const labelId = `${name}-label`;
  const value = props.value !== undefined ? props.value : field?.value;

  return (
    <FormControl
      className={className}
      error={hasError}
      fullWidth={true}
      margin="normal"
    >
      <InputLabel htmlFor={name} id={labelId}>
        {label}
      </InputLabel>
      <Select
        {...otherProps}
        classes={{
          select: classes.select,
        }}
        id={name}
        labelId={labelId}
        name={name}
        onChange={onChange || field?.onChange}
        onBlur={onBlur || field?.onBlur}
        value={value}
      >
        {items.map((item) => (
          <MenuItem key={item.id} value={item.id}>
            {item.name}
          </MenuItem>
        ))}
      </Select>
      {hasError && <FormHelperText>{helperText}</FormHelperText>}
    </FormControl>
  );
};
</file>

<file path="src/common/components/form/select/select.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const select = css`
  padding-top: ${theme.spacing(2)};
  padding-bottom: ${theme.spacing(2)};
`;
</file>

<file path="src/common/components/form/form.component.tsx">
import React from 'react';

interface Props {
  onSubmit: () => void;
  className?: string;
  children: React.ReactNode;
}

export const FormComponent: React.FunctionComponent<Props> = (props) => {
  const { onSubmit, className, children } = props;

  const handleSubmit = (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    onSubmit();
  };

  return (
    <form className={className} onSubmit={handleSubmit}>
      {children}
    </form>
  );
};
</file>

<file path="src/common/components/form/index.ts">
export * from './form.component';
export * from './text-field.component';
export * from './checkbox';
export * from './select';
</file>

<file path="src/common/components/form/text-field.component.tsx">
import React from 'react';
import { useField } from 'formik';
import { TextField as MuiTextField, TextFieldProps } from '@mui/material';

export const TextFieldComponent: React.FunctionComponent<TextFieldProps> = (
  props
) => {
  const [field, meta] = useField(props.name);
  const textFieldProps = Boolean(field) ? field : props;
  const hasError = Boolean(meta && meta.touched && meta.error);

  return (
    <MuiTextField
      {...props}
      name={textFieldProps.name}
      onChange={textFieldProps.onChange}
      onBlur={textFieldProps.onBlur}
      value={textFieldProps.value}
      error={hasError}
      helperText={hasError ? meta.error : ''}
      fullWidth={true}
      margin="normal"
    />
  );
};
</file>

<file path="src/common/components/pagination/index.ts">
export * from './pagination.component';
</file>

<file path="src/common/components/pagination/pagination.component.tsx">
import React from 'react';
import { Pagination } from '@mui/material';

interface Props {
  pageIndex: number;
  pageCount: number;
  onChange: (pageIndex: number) => void;
  className?: string;
}

export const PaginationComponent: React.FunctionComponent<Props> = (props) => {
  const { pageIndex, pageCount, onChange, className } = props;

  const handleChange = (event, value) => {
    onChange(value - 1);
  };

  return (
    <Pagination
      className={className}
      color="primary"
      count={pageCount}
      page={pageIndex + 1}
      onChange={handleChange}
    />
  );
};
</file>

<file path="src/common/components/search-bar/index.ts">
export * from './search-bar.component';
export * from './search-bar.hook';
</file>

<file path="src/common/components/search-bar/search-bar.component.tsx">
import React from 'react';
import { TextField } from '@mui/material';
import SearchIcon from '@mui/icons-material/Search';

interface LabelProps {
  placeholder: string;
}

interface Props {
  search: string;
  onSearch: (search: string) => void;
  labels: LabelProps;
  className?: string;
}

export const SearchBarComponent: React.FunctionComponent<Props> = (props) => {
  const { search, onSearch, labels, className } = props;

  return (
    <TextField
      className={className}
      value={search}
      onChange={(e) => onSearch(e.target.value)}
      placeholder={labels.placeholder}
      slotProps={{
        input: {
          startAdornment: <SearchIcon aria-label="Search icon" />,
        },
      }}
    />
  );
};
</file>

<file path="src/common/components/search-bar/search-bar.hook.ts">
import React from 'react';
import { useDebounce } from '#common/hooks';
import { filterByText } from '#common/helpers';

export const useSearchBar = <Entity, Key extends keyof Entity>(
  collection: Entity[],
  fields: Key[] | string[]
) => {
  const [search, setSearch] = React.useState('');
  const [filteredList, setFilteredList] = React.useState<Entity[]>(collection);
  React.useEffect(() => {
    setFilteredList(collection);
  }, [collection]);

  const debouncedSearch = useDebounce(search, 250);
  React.useEffect(() => {
    const newFilteredList = filterByText(collection, debouncedSearch, fields);
    setFilteredList(newFilteredList);
  }, [debouncedSearch]);

  return {
    search,
    onSearch: setSearch,
    filteredList,
  };
};
</file>

<file path="src/common/components/snackbar/index.ts">
export * from './snackbar.component';
export { SnackbarProvider } from './snackbar.context';
export { useSnackbarContext } from './snackbar.hook';
</file>

<file path="src/common/components/snackbar/snackbar.component.tsx">
import React from 'react';
import {
  Snackbar,
  SnackbarOrigin,
  SnackbarContent,
  IconButton,
} from '@mui/material';
import CloseIcon from '@mui/icons-material/Close';
import { SnackbarContext } from './snackbar.context';
import * as classes from './snackbar.styles';

interface Props {
  autoHideDuration?: number;
  position?: SnackbarOrigin;
}

export const SnackbarComponent: React.FunctionComponent<Props> = (props) => {
  const {
    position = {
      horizontal: 'right',
      vertical: 'top',
    },
    autoHideDuration = 3000,
  } = props;
  const { open, onClose, options } = React.useContext(SnackbarContext);

  return (
    <Snackbar
      anchorOrigin={position}
      open={open}
      autoHideDuration={autoHideDuration}
      onClose={onClose}
    >
      <SnackbarContent
        className={classes[options.variant]}
        message={options.message}
        action={[
          <IconButton key="close" color="inherit" onClick={onClose}>
            <CloseIcon />
          </IconButton>,
        ]}
      />
    </Snackbar>
  );
};
</file>

<file path="src/common/components/snackbar/snackbar.context.tsx">
import React from 'react';
import { SnackbarOptions } from './snackbar.vm';

interface Context {
  open: boolean;
  setOpen: (open: boolean) => void;
  onClose: (event?: React.SyntheticEvent, reason?: string) => void;
  options: SnackbarOptions;
  setOptions: (options: SnackbarOptions) => void;
}

export const SnackbarContext = React.createContext<Context>(null);

interface Props {
  children: React.ReactNode;
}

export const SnackbarProvider: React.FunctionComponent<Props> = (props) => {
  const { children } = props;
  const [open, setOpen] = React.useState(false);
  const [options, setOptions] = React.useState<SnackbarOptions>({
    message: '',
    variant: 'success',
  });

  const handleClose = (_, reason?: string) => {
    if (reason === 'clickaway') {
      return;
    }
    setOpen(false);
  };

  return (
    <SnackbarContext.Provider
      value={{
        open,
        setOpen,
        onClose: handleClose,
        options,
        setOptions,
      }}
    >
      {children}
    </SnackbarContext.Provider>
  );
};
</file>

<file path="src/common/components/snackbar/snackbar.hook.tsx">
import React from 'react';
import { SnackbarContext } from './snackbar.context';
import { Variant } from './snackbar.vm';

export const useSnackbarContext = () => {
  const { setOptions, setOpen } = React.useContext(SnackbarContext);

  return {
    showMessage: (message: string, variant: Variant) => {
      setOptions({ message, variant });
      setOpen(true);
    },
  };
};
</file>

<file path="src/common/components/snackbar/snackbar.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const success = css`
  &.MuiSnackbarContent-root {
    background-color: ${theme.palette.success.main};
  }
`;

export const error = css`
  &.MuiSnackbarContent-root {
    background-color: ${theme.palette.error.dark};
  }
`;

export const info = css`
  &.MuiSnackbarContent-root {
    background-color: ${theme.palette.info.main};
  }
`;

export const warning = css`
  &.MuiSnackbarContent-root {
    background-color: ${theme.palette.warning.main};
  }
`;
</file>

<file path="src/common/components/snackbar/snackbar.vm.ts">
export type Variant = 'success' | 'info' | 'warning' | 'error';

export interface SnackbarOptions {
  message: string;
  variant: Variant;
}
</file>

<file path="src/common/components/spinner/index.ts">
export * from './spinner.component';
</file>

<file path="src/common/components/spinner/spinner.component.tsx">
import React from 'react';
import { usePromiseTracker } from 'react-promise-tracker';
import { Modal } from '@mui/material';
import { ScaleLoader as Loader } from 'react-spinners';
import * as classes from './spinner.styles';

export const SpinnerComponent: React.FunctionComponent = () => {
  const { promiseInProgress } = usePromiseTracker();
  return (
    <Modal open={promiseInProgress} className={classes.modal}>
      <div className={classes.loaderContainer}>
        <Loader />
      </div>
    </Modal>
  );
};
</file>

<file path="src/common/components/spinner/spinner.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const modal = css`
  display: flex;
  justify-content: center;
  align-items: center;
`;

export const loaderContainer = css`
  border-radius: 15%;
  background-color: ${theme.palette.common.white};
  width: 100px;
  height: 100px;
  display: flex;
  justify-content: center;
  align-items: center;
  outline: none;
`;
</file>

<file path="src/common/components/table/components/body.component.spec.tsx">
import * as React from 'react';
import { Row } from '@tanstack/react-table';
import { render } from '@testing-library/react';
import { BodyComponent } from './body.component';
import { RowComponent } from './row.component';
import { CellComponent } from './cell.component';

interface TestRow {
  testRow: number;
}

describe('common/table/BodyComponent', () => {
  it('should render as expected', () => {
    // Arrange
    const props = {
      rows: [
        { original: { testRow: 1 } },
        { original: { testRow: 2 } },
        { original: { testRow: 3 } },
      ] as unknown as Row<TestRow>[],
      rowRenderer: (props) => (
        <RowComponent>
          <CellComponent>{props.row.testRow}</CellComponent>
        </RowComponent>
      ),
      prepareRow: vi.fn(),
    };

    // Act
    const { getByText } = render(<BodyComponent<TestRow> {...props} />);

    // Assert
    expect(getByText('1')).toBeInTheDocument();
    expect(getByText('2')).toBeInTheDocument();
    expect(getByText('3')).toBeInTheDocument();
  });
});
</file>

<file path="src/common/components/table/components/body.component.tsx">
import React from 'react';
import { Row } from '@tanstack/react-table';
import { TableBody } from '@mui/material';
import { RowRendererProps } from '../table.vm';

interface Props<T> {
  rows: Row<T>[];
  rowRenderer: (props: RowRendererProps<T>) => React.ReactNode;
}

export const BodyComponent = <T,>(props: Props<T>) => {
  const { rows, rowRenderer } = props;
  return (
    <TableBody>
      {rows.map((row) => (
        <React.Fragment key={row.id}>
          {rowRenderer({
            ...row,
            row: row.original,
          })}
        </React.Fragment>
      ))}
    </TableBody>
  );
};
</file>

<file path="src/common/components/table/components/cell.component.spec.tsx">
import * as React from 'react';
import { render } from '@testing-library/react';
import { CellComponent } from './cell.component';

describe('common/table/CellComponent', () => {
  it('should render as expected passing required properties', () => {
    // Arrange

    // Act
    const { getByText } = render(
      <CellComponent>
        <h1>Test content</h1>
      </CellComponent>
    );

    // Assert
    expect(getByText('Test content')).toBeInTheDocument();
  });
});
</file>

<file path="src/common/components/table/components/cell.component.tsx">
import React from 'react';
import { TableCell, TableCellProps } from '@mui/material';
import * as classes from './cell.styles';

export const CellComponent: React.FunctionComponent<TableCellProps> = (
  props
) => {
  const { children, align = 'left', ...rest } = props;
  return (
    <TableCell classes={{ head: classes.head }} align={align} {...rest}>
      {children}
    </TableCell>
  );
};
</file>

<file path="src/common/components/table/components/cell.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const head = css`
  background-color: ${theme.palette.primary.light};
  color: ${theme.palette.common.white};
`;
</file>

<file path="src/common/components/table/components/header.component.spec.tsx">
import * as React from 'react';
import { HeaderGroup } from '@tanstack/react-table';
import { render } from '@testing-library/react';
import { HeaderComponent } from './header.component';

describe('common/table/HeaderComponent', () => {
  it('should be rendered as expected passing required properties', () => {
    // Arrange
    const props = {
      headerGroups: [
        {
          id: '1',
          headers: [
            {
              column: {
                columnDef: {
                  header: 'Test label',
                },
              },
              getContext: vi.fn(),
            },
          ],
        },
      ] as unknown as HeaderGroup<any>[],
    };

    // Act
    const { getByText } = render(<HeaderComponent {...props} />);

    // Assert
    expect(getByText('Test label')).toBeInTheDocument();
  });

  it('should render two columns passing two columns', () => {
    // Arrange
    const props = {
      headerGroups: [
        {
          id: '1',
          headers: [
            {
              column: {
                columnDef: {
                  header: 'Test label 1',
                },
              },
              getContext: vi.fn(),
            },
          ],
        },
        {
          id: '2',
          headers: [
            {
              column: {
                columnDef: {
                  header: 'Test label 2',
                },
              },
              getContext: vi.fn(),
            },
          ],
        },
      ] as unknown as HeaderGroup<any>[],
    };

    // Act
    const { getByText } = render(<HeaderComponent {...props} />);

    // Assert
    expect(getByText('Test label 1')).toBeInTheDocument();
    expect(getByText('Test label 2')).toBeInTheDocument();
  });
});
</file>

<file path="src/common/components/table/components/header.component.tsx">
import React from 'react';
import { HeaderGroup, flexRender } from '@tanstack/react-table';
import { TableHead, TableRow } from '@mui/material';
import { CellComponent } from './cell.component';

interface Props {
  headerGroups: HeaderGroup<any>[];
}

export const HeaderComponent: React.FunctionComponent<Props> = (props) => {
  const { headerGroups } = props;
  return (
    <TableHead>
      {headerGroups.map((headerGroup) => (
        <TableRow key={headerGroup.id}>
          {headerGroup.headers.map((header) => (
            <CellComponent key={header.id}>
              {flexRender(header.column.columnDef.header, header.getContext())}
            </CellComponent>
          ))}
        </TableRow>
      ))}
    </TableHead>
  );
};
</file>

<file path="src/common/components/table/components/index.ts">
export * from './header.component';
export * from './body.component';
export * from './row.component';
export * from './cell.component';
</file>

<file path="src/common/components/table/components/row.component.spec.tsx">
import * as React from 'react';
import { render } from '@testing-library/react';
import { RowComponent } from './row.component';

describe('common/table/RowComponent', () => {
  it('should render as expected passing required properties', () => {
    // Arrange
    const props = {
      className: 'test-className',
      'data-testid': 'test-row',
    };

    // Act
    const { getByText, getByTestId } = render(
      <RowComponent {...props}>
        <td>{'Test rowData'}</td>
      </RowComponent>
    );

    // Assert
    expect(getByTestId(props['data-testid'])).toHaveClass(props.className);
    expect(getByText('Test rowData')).toBeInTheDocument();
  });

  it('should render a row component with two cells', () => {
    // Arrange
    const props = {
      className: 'test-className',
    };

    // Act
    const { getByText } = render(
      <RowComponent {...props}>
        <td>{'Test rowData 1'}</td>
        <td>{'Test rowData 2'}</td>
      </RowComponent>
    );

    // Assert
    expect(getByText('Test rowData 1')).toBeInTheDocument();
    expect(getByText('Test rowData 2')).toBeInTheDocument();
  });
});
</file>

<file path="src/common/components/table/components/row.component.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const root = css`
  &:nth-child(odd) {
    background-color: ${theme.palette.background.default};
  }
`;
</file>

<file path="src/common/components/table/components/row.component.tsx">
import React from 'react';
import { TableRow, TableRowProps } from '@mui/material';
import { cx } from '@emotion/css';
import * as classes from './row.component.styles';

interface Props extends TableRowProps {
  className?: string;
  'data-testid'?: string;
}

export const RowComponent: React.FunctionComponent<Props> = (props) => {
  const { className, children, ...rest } = props;
  return (
    <TableRow
      {...rest}
      className={cx(classes.root, className)}
      data-testid={props['data-testid']}
    >
      {children}
    </TableRow>
  );
};
</file>

<file path="src/common/components/table/index.ts">
export * from './table.container';
export * from './components/row.component';
export * from './components/cell.component';
export * from './table.vm';
</file>

<file path="src/common/components/table/table.component.tsx">
import React from 'react';
import { Table as TableProps } from '@tanstack/react-table';
import {
  Paper,
  TableContainer,
  Table,
  Button,
  Typography,
} from '@mui/material';
import { SearchBarComponent } from '../search-bar';
import { PaginationComponent } from '../pagination';
import { ConfirmationDialogComponent } from '../confirmation-dialog';
import { HeaderComponent, BodyComponent } from './components';
import { RowRendererProps, LabelProps } from './table.vm';
import * as classes from './table.styles';

interface Props<T extends object = {}> {
  tableProps: TableProps<T>;
  rowRenderer: (props: RowRendererProps<T>) => React.ReactNode;
  labels: LabelProps;
  enableSearch: boolean;
  search?: string;
  onSearch?: (search: string) => void;
  enablePagination: boolean;
  pageIndex?: number;
  pageCount?: number;
  goToPage?: (pageIndex: number) => void;
  onCreate?: () => void;
  onDelete?: () => void;
  itemToDeleteName?: string;
  isOpenConfirmation?: boolean;
  onCloseConfirmation?: () => void;
  className?: string;
}

export const TableComponent: React.FunctionComponent<Props> = (props) => {
  const {
    tableProps,
    rowRenderer,
    enableSearch,
    search,
    onSearch,
    enablePagination,
    pageIndex,
    pageCount,
    goToPage,
    onCreate,
    onDelete,
    itemToDeleteName,
    isOpenConfirmation,
    onCloseConfirmation,
    labels,
    className,
  } = props;

  return (
    <div className={className}>
      <div className={classes.root}>
        {enableSearch && (
          <SearchBarComponent
            className={classes.search}
            search={search}
            onSearch={onSearch}
            labels={{ placeholder: labels.searchPlaceholder }}
          />
        )}
        <TableContainer className={classes.table} component={Paper}>
          <Table>
            <HeaderComponent headerGroups={tableProps.getHeaderGroups()} />
            <BodyComponent
              rows={tableProps.getRowModel().rows}
              rowRenderer={rowRenderer}
            />
          </Table>
          {enablePagination && (
            <PaginationComponent
              className={classes.pagination}
              pageIndex={pageIndex}
              pageCount={pageCount}
              onChange={goToPage}
            />
          )}
        </TableContainer>
        {onCreate && (
          <Button variant="contained" color="primary" onClick={onCreate}>
            {labels.createButton}
          </Button>
        )}
        {onDelete && (
          <ConfirmationDialogComponent
            isOpen={isOpenConfirmation}
            onAccept={onDelete}
            onClose={onCloseConfirmation}
            title={labels.deleteTitle}
            labels={{
              closeButton: labels.closeButton,
              acceptButton: labels.acceptButton,
            }}
          >
            <Typography variant="body1">
              {labels.deleteContent({ itemName: itemToDeleteName })}
            </Typography>
          </ConfirmationDialogComponent>
        )}
      </div>
    </div>
  );
};
</file>

<file path="src/common/components/table/table.container.tsx">
import React from 'react';
import {
  useReactTable,
  getCoreRowModel,
  getPaginationRowModel,
} from '@tanstack/react-table';
import { useConfirmationDialog } from '../confirmation-dialog';
import {
  RowRendererProps,
  LabelProps,
  createEmptyLabelProps,
} from './table.vm';
import { TableComponent } from './table.component';
import { mapColumnListFromStringToColumn } from './table.mappers';

interface Props<T = {}> {
  columns: string[];
  rows: T[];
  rowRenderer: (props: RowRendererProps<T>) => React.ReactNode;
  enableSearch?: boolean;
  search?: string;
  onSearch?: (search: string) => void;
  enablePagination?: boolean;
  pageSize?: number;
  onCreate?: () => void;
  onEdit?: (id: string) => void;
  onDelete?: (id: string) => void;
  labels?: LabelProps;
  className?: string;
}

export const TableContainer: React.FunctionComponent<Props> = (props) => {
  const { className, enablePagination = false } = props;

  const labels = { ...createEmptyLabelProps(), ...props.labels };

  const columns = React.useMemo(
    () => mapColumnListFromStringToColumn(props.columns),
    [props.columns]
  );
  const data = React.useMemo(() => props.rows, [props.rows]);

  const table = useReactTable({
    columns,
    data,
    initialState: {
      pagination: {
        pageSize: props.pageSize,
      },
    },
    getCoreRowModel: getCoreRowModel(),
    getPaginationRowModel: getPaginationRowModel(),
  });
  const { pageIndex } = table.getState().pagination;
  const pageOptions = table.getPageOptions();
  const { isOpen, itemToDelete, onOpenDialog, onClose, onAccept } =
    useConfirmationDialog();

  const handleDelete = () => {
    if (props.onDelete) {
      props.onDelete(itemToDelete.id);
      onAccept();
    }
  };

  return (
    <TableComponent
      className={className}
      tableProps={table}
      rowRenderer={(rowProps) =>
        props.rowRenderer({
          ...rowProps,
          onEdit: props.onEdit,
          onDelete: Boolean(props.onDelete) ? onOpenDialog : undefined,
        })
      }
      labels={labels}
      enableSearch={props.enableSearch}
      search={props.search}
      onSearch={props.onSearch}
      enablePagination={Boolean(
        props.enablePagination && pageOptions.length > 1
      )}
      pageIndex={pageIndex}
      pageCount={pageOptions.length}
      goToPage={table.setPageIndex}
      onCreate={props.onCreate}
      onDelete={Boolean(props.onDelete) ? handleDelete : undefined}
      isOpenConfirmation={isOpen}
      onCloseConfirmation={onClose}
      itemToDeleteName={itemToDelete.name}
    />
  );
};
</file>

<file path="src/common/components/table/table.mappers.ts">
import { ColumnDef } from '@tanstack/react-table';
import { mapToCollection } from '#common/mappers';

const mapColumnFromStringToColumn = (column: string): ColumnDef<any> => ({
  accessorKey: column,
  header: column,
});

export const mapColumnListFromStringToColumn = (
  columns: string[]
): ColumnDef<any>[] => mapToCollection(columns, mapColumnFromStringToColumn);
</file>

<file path="src/common/components/table/table.styles.ts">
import { css } from '@emotion/css';

export const root = css`
  display: flex;
  flex-direction: column;
  width: 100%;
  align-items: flex-start;
  & > :nth-child(n + 2) {
    margin-top: 1rem;
  }
`;

export const search = css`
  width: 100%;
`;

export const table = css`
  display: flex;
  flex-direction: column;
`;

export const pagination = css`
  align-self: center;
  padding-top: 0.5rem;
  padding-bottom: 0.5rem;
`;
</file>

<file path="src/common/components/table/table.vm.ts">
import React from 'react';
import { Lookup } from '#common/models';

export interface RowRendererProps<T = {}> {
  row: T;
  onEdit?: (id: string) => void;
  onDelete?: (lookup: Lookup) => void;
}

export interface LabelProps {
  searchPlaceholder?: string;
  createButton?: string | React.ReactNode;
  deleteTitle?: string | React.ReactNode;
  deleteContent?: (props: { itemName: string }) => React.ReactNode;
  closeButton?: string;
  acceptButton?: string;
}

export const createEmptyLabelProps = (): LabelProps => ({
  searchPlaceholder: '',
  createButton: '',
  deleteTitle: '',
  deleteContent: undefined,
  closeButton: '',
  acceptButton: '',
});
</file>

<file path="src/common/components/tabs/index.ts">
export * from './tab-panel.component';
export * from './tab.component';
export * from './tab-list.component';
</file>

<file path="src/common/components/tabs/tab-list.component.tsx">
import React from 'react';
import { Tabs, TabsProps } from '@mui/material';

interface Props extends Omit<TabsProps, 'onChange'> {
  value: number;
  onChange: (value: number) => void;
}

export const TabListComponent: React.FunctionComponent<Props> = (props) => {
  const { value, onChange, ...otherProps } = props;

  const handleChange = (event, value) => {
    onChange(value);
  };

  return <Tabs {...otherProps} onChange={handleChange} value={value} />;
};
</file>

<file path="src/common/components/tabs/tab-panel.component.tsx">
import React from 'react';
import { Typography, Box } from '@mui/material';

interface Props {
  index: number;
  value: any;
  children: React.ReactNode;
}

export const TabPanelComponent: React.FunctionComponent<Props> = (props) => {
  const { children, value, index, ...other } = props;

  return (
    <Typography
      component="div"
      role="tabpanel"
      hidden={value !== index}
      id={`simple-tabpanel-${index}`}
      {...other}
    >
      {value === index && children}
    </Typography>
  );
};
</file>

<file path="src/common/components/tabs/tab.component.tsx">
import * as React from 'react';
import { Tab, TabProps } from '@mui/material';

type Props = TabProps;

export const TabComponent: React.FunctionComponent<Props> = (props) => {
  const { ...otherProps } = props;

  return <Tab {...otherProps} />;
};
</file>

<file path="src/common/components/index.ts">
export * from './dashboard';
export * from './footer';
export * from './form';
export * from './snackbar';
export * from './spinner';
export * from './table';
export * from './search-bar';
export * from './tabs';
</file>

<file path="src/common/constants/index.ts">
export * from './months';
</file>

<file path="src/common/constants/months.ts">
import { Lookup } from '../models';

export const monthList: Lookup[] = [
  {
    id: '1',
    name: 'Enero',
  },
  {
    id: '2',
    name: 'Febrero',
  },
  {
    id: '3',
    name: 'Marzo',
  },
  {
    id: '4',
    name: 'Abril',
  },
  {
    id: '5',
    name: 'Mayo',
  },
  {
    id: '6',
    name: 'Junio',
  },
  {
    id: '7',
    name: 'Julio',
  },
  {
    id: '8',
    name: 'Agosto',
  },
  {
    id: '9',
    name: 'Septiembre',
  },
  {
    id: '10',
    name: 'Octubre',
  },
  {
    id: '11',
    name: 'Noviembre',
  },
  {
    id: '12',
    name: 'Dicienbre',
  },
];
</file>

<file path="src/common/helpers/filter.helpers.spec.ts">
import { filterByFields, filterByText } from './filter.helpers';
import { Lookup } from '#common/models';

interface Item {
  id: string;
  name: string;
  age: number;
  cif: string;
  isBlocked: boolean;
  role?: Lookup;
}

describe('common/helpers/filter.helpers specs', () => {
  describe('filterByFields', () => {
    it('should return empty array when it feeds collection equals undefined', () => {
      // Arrange
      const collection: Item[] = undefined;
      const filter = undefined;

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return empty array when it feeds collection equals null', () => {
      // Arrange
      const collection: Item[] = null;
      const filter = undefined;

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return empty array when it feeds collection equals empty array', () => {
      // Arrange
      const collection: Item[] = [];
      const filter = undefined;

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return same array when it feeds collection with one item and filter equals undefined', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = undefined;

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and filter equals null', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = null;

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and filter with name and cif equals undefined', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: undefined,
        cif: undefined,
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and filter with name and cif equals null', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: null,
        cif: null,
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and filter with name and cif equals empty string', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: '',
        cif: '',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and filter with name equals "na" and cif equals empty string', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: 'na',
        cif: '',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and filter with name equals "name" and cif equals empty string', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: 'name',
        cif: '',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and filter with name equals "name 1" and cif equals empty string', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: 'name 1',
        age: 18,
        cif: '',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return empty array when it feeds collection with one item and filter with name equals "names" and cif equals empty string', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: 'names',
        cif: '',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return same array when it feeds collection with one item and filter with name equals empty string and cif equals "ci"', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: '',
        cif: 'ci',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and filter with name equals empty string and cif equals "cif 1"', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: '',
        cif: 'cif 1',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return empty array when it feeds collection with one item and filter with name equals empty string and cif equals "cif 12"', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: '',
        cif: 'cif 12',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return same array when it feeds collection with one item and filter with name equals "name" string and cif equals "cif"', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const filter = {
        name: 'name',
        cif: 'cif',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return two items array when it feeds collection with two items and filter with name equals "name" string and cif equals "cif"', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 19, cif: 'cif 2', isBlocked: false },
      ];
      const filter = {
        name: 'name',
        cif: 'cif',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return one item array when it feeds collection with two items and filter with name equals "name 1" string and cif equals "cif"', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 19, cif: 'cif 2', isBlocked: false },
      ];
      const filter = {
        name: 'name 1',
        age: 18,
        cif: 'cif',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ]);
    });

    it('should return one item array when it feeds collection with two items and filter with isBlocked equals true ', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: true },
        { id: '2', name: 'name 2', age: 19, cif: 'cif 2', isBlocked: false },
      ];
      const filter = {
        isBlocked: true,
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: true },
      ]);
    });

    it('should return one item array when it feeds collection with two items and filter with isBlocked equals false ', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: true },
        { id: '2', name: 'name 2', age: 19, cif: 'cif 2', isBlocked: false },
      ];
      const filter = {
        isBlocked: false,
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([
        { id: '2', name: 'name 2', age: 19, cif: 'cif 2', isBlocked: false },
      ]);
    });

    it('should return empty array when it feeds collection with two items with null values and filter with name equals "test"', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: null, age: 18, cif: 'cif 1', isBlocked: true },
        { id: '2', name: null, age: 19, cif: 'cif 2', isBlocked: false },
      ];
      const filter = {
        name: 'test',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return first item when it feeds collection with two items and filter with age equals "18"', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: null, age: 18, cif: 'cif 1', isBlocked: true },
        { id: '2', name: null, age: 19, cif: 'cif 2', isBlocked: false },
      ];
      const filter = {
        age: 18,
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([
        { id: '1', name: null, age: 18, cif: 'cif 1', isBlocked: true },
      ]);
    });

    it('should return first item when it feeds collection with two items and filter using dot annotation with role.name equals "admin" and it matchs with first item', () => {
      // Arrange
      const collection: Item[] = [
        {
          id: '1',
          name: null,
          age: 18,
          cif: 'cif 1',
          isBlocked: true,
          role: { id: '1', name: 'admin' },
        },
        {
          id: '2',
          name: null,
          age: 19,
          cif: 'cif 2',
          isBlocked: false,
          role: { id: '2', name: 'user' },
        },
      ];

      const filter = {
        'role.name': 'admin',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([
        {
          id: '1',
          name: null,
          age: 18,
          cif: 'cif 1',
          isBlocked: true,
          role: { id: '1', name: 'admin' },
        },
      ]);
    });

    it('should return first item when it feeds collection with two items and filter using nested object with role.name equals "admin" and it matchs with first item', () => {
      // Arrange
      const collection: Item[] = [
        {
          id: '1',
          name: null,
          age: 18,
          cif: 'cif 1',
          isBlocked: true,
          role: { id: '1', name: 'admin' },
        },
        {
          id: '2',
          name: null,
          age: 19,
          cif: 'cif 2',
          isBlocked: false,
          role: { id: '2', name: 'user' },
        },
      ];

      const filter = {
        role: {
          id: '1',
          name: 'admin',
        },
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([
        {
          id: '1',
          name: null,
          age: 18,
          cif: 'cif 1',
          isBlocked: true,
          role: { id: '1', name: 'admin' },
        },
      ]);
    });
  });

  describe('filterByText', () => {
    it('should return empty array when it feeds collection equals undefined', () => {
      // Arrange
      const collection: Item[] = undefined;
      const text = undefined;
      const fields = undefined;

      // Act
      const result = filterByText(collection, text, fields);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return empty array when it feeds collection equals null', () => {
      // Arrange
      const collection: Item[] = null;
      const text = undefined;
      const fields = undefined;

      // Act
      const result = filterByText(collection, text, fields);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return empty array when it feeds collection equals empty array', () => {
      // Arrange
      const collection: Item[] = [];
      const text = undefined;
      const fields = undefined;

      // Act
      const result = filterByText(collection, text, fields);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return same array when it feeds collection with one item,text equals undefined and fields equals undefined', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = undefined;
      const fields = undefined;

      // Act
      const result = filterByText(collection, text, fields);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals undefined and fields has id', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = undefined;

      // Act
      const result = filterByText(collection, text, ['id']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals undefined and fields has name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = undefined;

      // Act
      const result = filterByText(collection, text, ['name']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals undefined and fields has age', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = undefined;

      // Act
      const result = filterByText(collection, text, ['age']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals undefined and fields has cif', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = undefined;

      // Act
      const result = filterByText(collection, text, ['cif']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals undefined and fields has isBlocked', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = undefined;

      // Act
      const result = filterByText(collection, text, ['isBlocked']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals null and fields has id', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = null;

      // Act
      const result = filterByText(collection, text, ['id']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals null and fields has name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = null;

      // Act
      const result = filterByText(collection, text, ['name']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals null and fields has age', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = null;

      // Act
      const result = filterByText(collection, text, ['age']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals null and fields has cif', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = null;

      // Act
      const result = filterByText(collection, text, ['cif']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals null and fields has isBlocked', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = null;

      // Act
      const result = filterByText(collection, text, ['isBlocked']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals empty string and fields has id', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '';

      // Act
      const result = filterByText(collection, text, ['id']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals empty string and fields has name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '';

      // Act
      const result = filterByText(collection, text, ['name']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals empty string and fields has age', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '';

      // Act
      const result = filterByText(collection, text, ['age']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals empty string and fields has cif', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '';

      // Act
      const result = filterByText(collection, text, ['cif']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item, text equals empty string and fields has isBlocked', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '';

      // Act
      const result = filterByText(collection, text, ['isBlocked']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and text equals "1" and fields equals id', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '1';

      // Act
      const result = filterByText(collection, text, ['id']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and text equals "na" and fields equals name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = 'na';

      // Act
      const result = filterByText(collection, text, ['name']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and text equals "1" and fields equals age', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '1';

      // Act
      const result = filterByText(collection, text, ['age']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and text equals "1" and fields equals cif', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = 'c';

      // Act
      const result = filterByText(collection, text, ['cif']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and text equals "fal" and fields equals isBlocked', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = 'fal';

      // Act
      const result = filterByText(collection, text, ['isBlocked']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return empty array when it feeds collection with one item and text equals "2" and fields equals id', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '2';

      // Act
      const result = filterByText(collection, text, ['id']);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return same array when it feeds collection with one item and text equals "1" and fields equals id and name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '1';

      // Act
      const result = filterByText(collection, text, ['id', 'name']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and text equals "1" and fields equals id and age', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '1';

      // Act
      const result = filterByText(collection, text, ['id', 'age']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and text equals "1" and fields equals id and cif', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '1';

      // Act
      const result = filterByText(collection, text, ['id', 'cif']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and text equals "1" and fields equals id and isBlocked', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = '1';

      // Act
      const result = filterByText(collection, text, ['id', 'isBlocked']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return same array when it feeds collection with one item and text equals "name" and fields equals id and name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ];
      const text = 'name';

      // Act
      const result = filterByText(collection, text, ['id', 'name']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return second item array when it feeds collection with two items and text equals "2" and fields equals id and name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ];
      const text = '2';

      // Act
      const result = filterByText(collection, text, ['id', 'name']);

      // Assert
      expect(result).toEqual([
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ]);
    });

    it('should return same array when it feeds collection with two items and text equals "name" and fields equals id and name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ];
      const text = 'name';

      // Act
      const result = filterByText(collection, text, ['id', 'name']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return firt item array when it feeds collection with two items and text equals "name 1" and fields equals id and name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ];
      const text = 'name 1';

      // Act
      const result = filterByText(collection, text, ['id', 'name']);

      // Assert
      expect(result).toEqual([
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ]);
    });

    it('should return empty array when it feeds collection with two items and text equals "name 3" and fields equals id and name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ];
      const text = 'name 3';

      // Act
      const result = filterByText(collection, text, ['id', 'name']);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return empty array when it feeds collection with two items and text equals "true" and fields equals id and name', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ];
      const text = 'true';

      // Act
      const result = filterByText(collection, text, ['id', 'name']);

      // Assert
      expect(result).toEqual([]);
    });

    it('should return same array when it feeds collection with two items and text equals "8" and fields equals id, name and age', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ];
      const text = '8';

      // Act
      const result = filterByText(collection, text, ['id', 'name', 'age']);

      // Assert
      expect(result).toEqual(collection);
    });

    it('should return first item array when it feeds collection with two items and text equals "18" and fields equals id, name and age', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ];
      const text = '18';

      // Act
      const result = filterByText(collection, text, ['id', 'name', 'age']);

      // Assert
      expect(result).toEqual([
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ]);
    });

    it('should return second item array when it feeds collection with two items and text equals "28" and fields equals id, name and age', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ];
      const text = '28';

      // Act
      const result = filterByText(collection, text, ['id', 'name', 'age']);

      // Assert
      expect(result).toEqual([
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: true },
      ]);
    });

    it('should return second item array when it feeds collection with two items and role.name equals "admin" and fields equals "role.name"', () => {
      // Arrange
      const collection: Item[] = [
        {
          id: '1',
          name: 'name 1',
          age: 18,
          cif: 'cif 1',
          isBlocked: false,
          role: {
            id: '2',
            name: 'user',
          },
        },
        {
          id: '2',
          name: 'name 2',
          age: 28,
          cif: 'cif 2',
          isBlocked: true,
          role: {
            id: '1',
            name: 'admin',
          },
        },
      ];
      const text = 'admin';

      // Act
      const result = filterByText(collection, text, ['role.name']);

      // Assert
      expect(result).toEqual([
        {
          id: '2',
          name: 'name 2',
          age: 28,
          cif: 'cif 2',
          isBlocked: true,
          role: {
            id: '1',
            name: 'admin',
          },
        },
      ]);
    });
  });

  describe('upper case', () => {
    it('should return first item when it feeds collection with two items and filter with name equals "NAME 1" using filterByFields', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: false },
      ];
      const filter = {
        name: 'NAME 1',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ]);
    });

    it('should return first item when it feeds collection with two items, text equals "NAME 1" and fields has name using filterByText', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: false },
      ];
      const text = 'NAME 1';

      // Act
      const result = filterByText(collection, text, ['name']);

      // Assert
      expect(result).toEqual([
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ]);
    });
  });

  describe('ignore accents', () => {
    it('should return first item when it feeds collection with two items and filter with name equals "náme 1" using filterByFields', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: false },
      ];
      const filter = {
        name: 'náme 1',
      };

      // Act
      const result = filterByFields(collection, filter);

      // Assert
      expect(result).toEqual([
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ]);
    });

    it('should return first item when it feeds collection with two items, text equals "náme 1" and fields has name using filterByText', () => {
      // Arrange
      const collection: Item[] = [
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
        { id: '2', name: 'name 2', age: 28, cif: 'cif 2', isBlocked: false },
      ];
      const text = 'náme 1';

      // Act
      const result = filterByText(collection, text, ['name']);

      // Assert
      expect(result).toEqual([
        { id: '1', name: 'name 1', age: 18, cif: 'cif 1', isBlocked: false },
      ]);
    });
  });
});
</file>

<file path="src/common/helpers/filter.helpers.ts">
import get from 'lodash.get';
import { flattenObject } from './object.helpers';

const getStringLowerCase = (value: string): string =>
  value ? value.toLowerCase().trim() : '';

const removeAccents = (value: string) =>
  Boolean(value.normalize)
    ? value.normalize('NFD').replace(/[\u0300-\u036f]/g, '')
    : value;

const formatValue = value => {
  const stringValue = getStringLowerCase(`${value}`);
  return removeAccents(stringValue);
};

const filterByField = <Entity, Filter>(
  entity: Entity,
  filter: Filter,
  field: string
) => {
  const filterField = formatValue(filter[field]);
  const entityField = formatValue(get(entity, field));

  return !filterField || entityField.indexOf(filterField) >= 0;
};

const isUndefinedOrNull = value => value === undefined || value === null;

const matchEveryField = <Entity>(
  item: Entity,
  filter: Partial<Entity>
): boolean =>
  Object.keys(filter).every(
    field =>
      isUndefinedOrNull(filter[field]) || filterByField(item, filter, field)
  );

type Filter<Entity> = Partial<Entity> | { [key: string]: string };
type FilterFn = <Entity>(item: Entity, filter: Filter<Entity>) => boolean;

export const filterByFields = <Entity>(
  collection: Entity[],
  filter: Filter<Entity>,
  filterFn: FilterFn = matchEveryField
) => {
  const flatFilter = flattenObject(filter);

  return Array.isArray(collection)
    ? collection.filter(item => !flatFilter || filterFn(item, flatFilter))
    : [];
};

const matchSomeField = <Entity>(item: Entity, filter: Partial<Entity>) =>
  Object.keys(filter).some(
    field =>
      isUndefinedOrNull(filter[field]) || filterByField(item, filter, field)
  );

export const filterByText = <Entity, Key extends keyof Entity>(
  collection: Entity[],
  text: string,
  fields: Key[] | string[]
) => {
  const filter =
    Array.isArray(fields) &&
    (fields as Key[]).reduce(
      (newFilter, field) => ({
        ...newFilter,
        [field]: text,
      }),
      {}
    );

  return filterByFields(collection, filter, matchSomeField);
};
</file>

<file path="src/common/helpers/index.ts">
export * from './filter.helpers';
export * from './is-edit-mode.helper';
</file>

<file path="src/common/helpers/is-edit-mode.helper.spec.ts">
import { isEditModeHelper } from './is-edit-mode.helper';

describe('/common/helpers/is-edit-model.helper.spect', () => {
  it('should return false when feeding id equals null', () => {
    // Arrange
    const id = null;

    // Act
    const result = isEditModeHelper(id);

    // Assert
    expect(result).not.toBeTruthy();
  });

  it('should return false when feeding id equals undefined', () => {
    // Arrange
    const id = undefined;

    // Act
    const result = isEditModeHelper(id);

    // Assert
    expect(result).not.toBeTruthy();
  });

  it('should return false when feeding id equals empty', () => {
    // Arrange
    const id = '';

    // Act
    const result = isEditModeHelper(id);

    // Assert
    expect(result).not.toBeTruthy();
  });

  it('should return false when feeding id equals 0', () => {
    // Arrange
    const id = '0';

    // Act
    const result = isEditModeHelper(id);

    // Assert
    expect(result).not.toBeTruthy();
  });

  it('should return true when feeding id equals string 1', () => {
    // Arrange
    const id = '1';

    // Act
    const result = isEditModeHelper(id);

    // Assert
    expect(result).toBeTruthy();
  });

  it('should return true when feeding id equals string Abc1Io', () => {
    // Arrange
    const id = 'Abc1Io';

    // Act
    const result = isEditModeHelper(id);

    // Assert
    expect(result).toBeTruthy();
  });

  it('should return true when feeding id equals number 1', () => {
    // Arrange
    const id = 1;

    // Act
    const result = isEditModeHelper(id);

    // Assert
    expect(result).toBeTruthy();
  });
});
</file>

<file path="src/common/helpers/is-edit-mode.helper.ts">
export const isEditModeHelper = id => {
  const idByDefault = '0';
  const isIdDefined = Boolean(id);

  return isIdDefined && id !== idByDefault;
};
</file>

<file path="src/common/helpers/object.helpers.spec.ts">
import { flattenObject } from './object.helpers';

describe('common/helpers/object.helpers specs', () => {
  describe('flattenObject', () => {
    it('should return null when if feeds entity equals undefined', () => {
      // Arrange
      const entity = undefined;

      // Act
      const result = flattenObject(entity);

      // Assert
      expect(result).toBeNull();
    });

    it('should return null when if feeds entity equals null', () => {
      // Arrange
      const entity = null;

      // Act
      const result = flattenObject(entity);

      // Assert
      expect(result).toBeNull();
    });

    it('should return same object when if feeds entity equals simple object', () => {
      // Arrange
      const entity = {
        id: '1',
        name: 'test',
      };

      // Act
      const result = flattenObject(entity);

      // Assert
      expect(result).toEqual(entity);
    });

    it('should return same object when if feeds entity equals simple object with undefined, null and empty string values, array, boolean', () => {
      // Arrange
      const entity = {
        id: undefined,
        name: null,
        role: '',
        companies: [],
        profiles: ['1'],
        isActive: false,
      };

      // Act
      const result = flattenObject(entity);

      // Assert
      expect(result).toEqual(entity);
    });

    it('should return same object when if feeds entity equals object with key names with dot annotation', () => {
      // Arrange
      const entity = {
        id: '1',
        name: 'test name',
        'role.name': 'test role name',
      };

      // Act
      const result = flattenObject(entity);

      // Assert
      expect(result).toEqual(entity);
    });

    it('should return flat object when if feeds entity equals object with nested object one deep level', () => {
      // Arrange
      const entity = {
        id: '1',
        name: 'test name',
        role: {
          name: 'test role name',
        },
      };

      // Act
      const result = flattenObject(entity);

      // Assert
      const expectedResult = {
        id: '1',
        name: 'test name',
        'role.name': 'test role name',
      };
      expect(result).toEqual(expectedResult);
    });

    it('should return flat object when if feeds entity equals object with nested object two deep levels', () => {
      // Arrange
      const entity = {
        id: '1',
        name: 'test name',
        user: {
          role: {
            id: 'test role id',
            name: 'test role name',
          },
        },
      };

      // Act
      const result = flattenObject(entity);

      // Assert
      const expectedResult = {
        id: '1',
        name: 'test name',
        'user.role.id': 'test role id',
        'user.role.name': 'test role name',
      };
      expect(result).toEqual(expectedResult);
    });

    it('should return flat object when if feeds entity equals object with key names with dot annotation, with nested object one, two and three deep levels', () => {
      // Arrange
      const entity = {
        id: '1',
        name: 'test name',
        'country.name': 'test country name',
        user: {
          id: 'test user id',
          role: {
            id: 'test role id',
            name: 'test role name',
            type: {
              name: 'test type name',
            },
          },
        },
      };

      // Act
      const result = flattenObject(entity);

      // Assert
      const expectedResult = {
        id: '1',
        name: 'test name',
        'country.name': 'test country name',
        'user.id': 'test user id',
        'user.role.id': 'test role id',
        'user.role.name': 'test role name',
        'user.role.type.name': 'test type name',
      };
      expect(result).toEqual(expectedResult);
    });
  });
});
</file>

<file path="src/common/helpers/object.helpers.ts">
export const flattenObject = <Entity extends object = {}>(
  entity: Entity,
  key?: string
) =>
  Boolean(entity)
    ? Object.entries(entity).reduce((flatObject, [subKey, value]) => {
        const composedKey = key ? `${key}.${subKey}` : subKey;
        const newValue =
          typeof value !== 'object' || value === null || Array.isArray(value)
            ? { [composedKey]: value }
            : flattenObject(value, composedKey);
        return {
          ...flatObject,
          ...newValue,
        };
      }, {})
    : null;
</file>

<file path="src/common/hooks/debounce.hook.ts">
import React from 'react';

export const useDebounce = (value, timeout) => {
  const [debouncedValue, setDebouncedValue] = React.useState(value);

  React.useEffect(() => {
    const timerId = setTimeout(() => {
      setDebouncedValue(value);
    }, timeout);
    return () => {
      clearTimeout(timerId);
    };
  }, [value, timeout]);

  return debouncedValue;
};
</file>

<file path="src/common/hooks/index.ts">
export * from './debounce.hook';
</file>

<file path="src/common/mappers/collection.mapper.spec.ts">
import { mapToCollection } from './collection.mapper';

describe('common/mappers/collection.mapper spec', () => {
  describe('mapToCollection', () => {
    it('shoudl return empty array when it feeds collectio equals undefined', () => {
      //Arrange
      const value = undefined;

      const map = item => ({
        value1: item.value1 * 2,
        value2: item.value2 + 'C',
      });

      //Act

      const result = mapToCollection(value, map);

      //Assert
      const valores = [];
      expect(result).toEqual(valores);
    });

    it('shoudl return empty array when it feeds collectio equals null', () => {
      //Arrange
      const value = null;

      const map = item => ({
        value1: item.value1 * 2,
        value2: item.value2 + 'C',
      });

      //Act

      const result = mapToCollection(value, map);

      //Assert
      const valores = [];
      expect(result).toEqual(valores);
    });

    it('shoudl return empty array when it feeds collectio equals empty array', () => {
      //Arrange
      const value = [];

      const map = item => ({
        value1: item.value1 * 2,
        value2: item.value2 + 'C',
      });

      //Act

      const result = mapToCollection(value, map);

      //Assert
      const valores = [];
      expect(result).toEqual(valores);
    });

    it('should return mapped collection when it feeds collection and function to map ', () => {
      //Arrange
      const collection = [
        {
          value1: 1,
          value2: 'A',
        },
        {
          value1: 2,
          value2: 'B',
        },
      ];

      const map = item => ({
        value1: item.value1 * 2,
        value2: item.value2 + 'C',
      });

      //Act
      const result = mapToCollection(collection, map);

      //Assert
      const valores = [
        {
          value1: 2,
          value2: 'AC',
        },
        {
          value1: 4,
          value2: 'BC',
        },
      ];
      expect(result).toEqual(valores);
    });
  });
});
</file>

<file path="src/common/mappers/collection.mapper.ts">
export const mapToCollection = <A, B>(
  collection: A[],
  mapItemFn: (item: A) => B
): B[] => (Array.isArray(collection) ? collection.map(mapItemFn) : []);
</file>

<file path="src/common/mappers/index.ts">
export * from './collection.mapper';
</file>

<file path="src/common/models/index.ts">
export * from './lookup';
</file>

<file path="src/common/models/lookup.ts">
export interface Lookup {
  id: string;
  name: string;
}

export const createEmptyLookup = (): Lookup => ({
  id: '',
  name: '',
});
</file>

<file path="src/common/test/index.ts">
export * from './render-with-router';
</file>

<file path="src/common/test/render-with-router.tsx">
import React from 'react';
import { render } from '@testing-library/react';
import { HashRouter, Routes } from 'react-router-dom';

export const renderWithRouter = (component, routes) => {
  return {
    ...render(
      <HashRouter>
        <Routes>{routes}</Routes>
        {component}
      </HashRouter>
    ),
  };
};
</file>

<file path="src/common-app/app-bar/components/index.ts">
export * from './side-menu.component';
</file>

<file path="src/common-app/app-bar/components/side-list.component.tsx">
import React from 'react';
import { List, ListItemButton, ListItemText } from '@mui/material';
import * as classes from './side-list.styles';

interface Props {
  onClick: () => void;
  onKeyDown: () => void;
}

// TODO: Implement menu items
export const SideListComponent: React.FC<Props> = ({ onClick, onKeyDown }) => (
  <div
    className={classes.list}
    role="presentation"
    onClick={onClick}
    onKeyDown={onKeyDown}
  >
    <List>
      {['Calendario', 'Proyectos', 'Vacaciones'].map((text) => (
        <ListItemButton key={text}>
          <ListItemText primary={text} />
        </ListItemButton>
      ))}
    </List>
  </div>
);
</file>

<file path="src/common-app/app-bar/components/side-list.styles.ts">
import { css } from '@emotion/css';

export const list = css`
  width: 250px;
`;
</file>

<file path="src/common-app/app-bar/components/side-menu.component.tsx">
import React from 'react';
import { Drawer } from '@mui/material';
import { SideListComponent } from './side-list.component';

interface Props {
  open: boolean;
  onClick: () => void;
}

export const SideMenuComponent: React.FC<Props> = ({ open, onClick }) => (
  <Drawer open={open} onClose={onClick}>
    <SideListComponent onClick={onClick} onKeyDown={onClick} />
  </Drawer>
);
</file>

<file path="src/common-app/app-bar/app-bar.component.tsx">
import React from 'react';
import {
  AppBar,
  Divider,
  IconButton,
  Menu,
  MenuItem,
  Toolbar,
  Typography,
} from '@mui/material';
import AccountCircle from '@mui/icons-material/AccountCircle';
import MenuIcon from '@mui/icons-material/Menu';
import { literals } from '#core/i18n';
import { AuthContext, useLogout } from '#common-app/auth';
import { SideMenuComponent } from './components';
import { useMenu } from './app-bar.hook';
import * as classes from './app-bar.styles';

// TODO: Pending to implement dynamic menu
export const AppBarComponent: React.FC = () => {
  const [open, setOpen] = React.useState(false);
  const { userName } = React.useContext(AuthContext);
  const { onLogout } = useLogout();
  const { isOpen, menuElement, onOpenMenu, onCloseMenu } = useMenu();

  const handleLogout = () => {
    onCloseMenu();
    onLogout();
  };

  const handleSideMenu = () => setOpen((prev) => !prev);

  return (
    <>
      <AppBar position="static">
        <Toolbar>
          <IconButton
            edge="start"
            className={classes.menuButton}
            color="inherit"
            aria-label="menu"
            onClick={handleSideMenu}
          >
            <MenuIcon />
          </IconButton>
          <Typography variant="h6" color="inherit">
            Origin - Project tracker
          </Typography>
          <Typography
            variant="h6"
            color="inherit"
            className={classes.loginText}
          >
            {literals.global.welcome} {userName}
          </Typography>
          <IconButton color="inherit" aria-label="menu" onClick={onOpenMenu}>
            <AccountCircle />
          </IconButton>
          <Menu open={isOpen} onClose={onCloseMenu} anchorEl={menuElement}>
            <MenuItem onClick={handleLogout}>
              {literals.components.fields.myProfile}
            </MenuItem>
            <Divider />
            <MenuItem onClick={handleLogout}>
              {literals.components.actions.logout}
            </MenuItem>
          </Menu>
        </Toolbar>
      </AppBar>
      <SideMenuComponent open={open} onClick={handleSideMenu} />
    </>
  );
};
</file>

<file path="src/common-app/app-bar/app-bar.hook.ts">
import React from 'react';

export const useMenu = (): {
  isOpen: boolean;
  menuElement: HTMLElement;
  onOpenMenu: (event: React.MouseEvent<HTMLElement>) => void;
  onCloseMenu: () => void;
} => {
  const [isOpen, setOpen] = React.useState(false);
  const [menuElement, setMenuElement] = React.useState<HTMLElement>(null);

  const onOpenMenu = (e: React.MouseEvent<HTMLElement>) => {
    setOpen(true);
    setMenuElement(e.currentTarget);
  };

  const onCloseMenu = () => {
    setOpen(false);
    setMenuElement(null);
  };

  return {
    isOpen,
    menuElement,
    onOpenMenu,
    onCloseMenu,
  };
};
</file>

<file path="src/common-app/app-bar/app-bar.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const menuButton = css`
  margin-right: ${theme.spacing(1)};
`;

export const loginText = css`
  margin-left: auto;
  padding-right: ${theme.spacing(1)};
`;
</file>

<file path="src/common-app/app-bar/index.ts">
export * from './app-bar.component';
</file>

<file path="src/common-app/auth/auth-route.component.tsx">
import React from 'react';
import { Route, RouteProps, useLocation } from 'react-router-dom';
import { AuthContext } from './auth.context';
import { useAuthRedirect } from './auth.hooks';

interface Props {
  children: React.ReactNode;
}

export const AuthRoute: React.FC<Props> = (props) => {
  const { children } = props;
  const { isAuthenticated } = React.useContext(AuthContext);
  const location = useLocation();
  const { onRedirect } = useAuthRedirect();

  React.useEffect(() => {
    if (!isAuthenticated) {
      onRedirect();
    }
  }, [location.pathname]);

  return <>{children}</>;
};
</file>

<file path="src/common-app/auth/auth.context.tsx">
import React from 'react';
import { UserSession, createEmptyUserSession } from './auth.vm';

interface Context extends UserSession {
  setUserSession: (userSession: UserSession) => void;
  isAuthenticated: boolean;
}

const noUserLogin = 'no user login';

export const AuthContext = React.createContext<Context>({
  userName: noUserLogin,
  isAuthenticated: false,
  setUserSession: () =>
    console.warn(
      'If you area reading this, likely you forgot to add the provider on top of your app'
    ),
});

interface Props {
  children: React.ReactNode
}
export const AuthProvider: React.FC<Props> = ({ children }) => {
  const [userSession, setUserSession] = React.useState<UserSession>(
    createEmptyUserSession()
  );

  const isAuthenticated = React.useMemo<boolean>(
    () => userSession.userName !== noUserLogin && userSession.userName !== '',
    [userSession.userName]
  );

  return (
    <AuthContext.Provider
      value={{
        userName: userSession.userName,
        isAuthenticated,
        setUserSession,
      }}
    >
      {children}
    </AuthContext.Provider>
  );
};
</file>

<file path="src/common-app/auth/auth.hooks.ts">
import React from 'react';
import { useNavigate } from 'react-router-dom';
import { routes } from '#core/router';
import { AuthContext } from './auth.context';
import { createEmptyUserSession } from './auth.vm';

export const useAuthRedirect = (): { onRedirect: () => void } => {
  const navigate = useNavigate();

  const onRedirect = React.useCallback(() => {
    navigate(routes.login);
  }, []);

  return { onRedirect };
};

export const useLogout = (): { onLogout: () => void } => {
  const { setUserSession } = React.useContext(AuthContext);
  const { onRedirect } = useAuthRedirect();

  const handleLogout = React.useCallback(() => {
    setUserSession(createEmptyUserSession());
    onRedirect();
  }, []);

  return {
    onLogout: handleLogout,
  };
};
</file>

<file path="src/common-app/auth/auth.vm.ts">
export interface UserSession {
  userName: string;
}

export const createEmptyUserSession = (): UserSession => ({
  userName: '',
});
</file>

<file path="src/common-app/auth/index.ts">
export * from './auth-route.component';
export * from './auth.context';
export * from './auth.hooks';
export * from './auth.vm';
</file>

<file path="src/common-app/command-footer/translations/en.ts">
import { keys } from './keys';

export const en = {
  [keys.buttons.cancel]: 'Cancel',
  [keys.buttons.save]: 'Save',
};
</file>

<file path="src/common-app/command-footer/translations/es.ts">
import { keys } from './keys';

export const es = {
  [keys.buttons.cancel]: 'Atrás',
  [keys.buttons.save]: 'Guardar',
};
</file>

<file path="src/common-app/command-footer/translations/index.ts">
export * from './keys';
export * from './en';
export * from './es';
</file>

<file path="src/common-app/command-footer/translations/keys.ts">
export const keys = {
  buttons: {
    cancel: 'buttons.cancel',
    save: 'buttons.save',
  },
};
</file>

<file path="src/common-app/command-footer/command-footer.component.tsx">
import * as React from 'react';
import { cx } from '@emotion/css';
import { Button } from '@mui/material';
import * as classes from './command-footer.style';

interface LabelProps {
  cancelButton?: string;
  saveButton?: string;
}

interface Props {
  onCancel: () => void;
  onSave?: () => void;
  labels?: LabelProps;
  className?: string;
}

export const CommandFooterComponent: React.FunctionComponent<Props> = (
  props
) => {
  const { onCancel, onSave, className } = props;
  const labels: LabelProps = {
    cancelButton: 'Cancelar',
    saveButton: 'Guardar',
    ...props.labels,
  };

  return (
    <div className={cx(classes.footerButtonsContainer, className)}>
      <Button variant="contained" color="primary" onClick={onCancel}>
        {labels.cancelButton}
      </Button>
      <Button
        type="submit"
        variant="contained"
        color="primary"
        onClick={() => {
          if (onSave) onSave();
        }}
      >
        {labels.saveButton}
      </Button>
    </div>
  );
};
</file>

<file path="src/common-app/command-footer/command-footer.style.ts">
import { css } from '@emotion/css';

export const footerButtonsContainer = css`
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  margin-top: 1rem;
`;
</file>

<file path="src/common-app/command-footer/index.ts">
export * from './command-footer.component';
</file>

<file path="src/core/router/index.ts">
export * from './router.component';
export * from './routes';
</file>

<file path="src/core/router/router.component.tsx">
import React from 'react';
import { HashRouter, Routes, Route } from 'react-router-dom';
import { AuthRoute } from '#common-app/auth';
import { routes } from './routes';
import {
  LoginScene,
  SubmoduleListScene,
  ProjectListScene,
  EmployeeListScene,
  ProjectScene,
  EmployeeScene,
} from '#scenes';

export const RouterComponent: React.FunctionComponent = () => {
  return (
    <HashRouter>
      <Routes>
        <Route path={routes.root} element={<LoginScene />} />
        <Route path={routes.login} element={<LoginScene />} />
        <Route
          path={routes.submoduleList}
          element={<AuthRoute>
            <SubmoduleListScene />
          </AuthRoute>}
        />
        <Route path={routes.projects} element={<ProjectListScene />} />
        <Route path={routes.employees} element={<EmployeeListScene />} />
        <Route path={routes.editProject()} element={<ProjectScene />} />
        <Route path={routes.editEmployee()} element={<EmployeeScene />} />
      </Routes>
    </HashRouter>
  );
};
</file>

<file path="src/core/router/routes.ts">
import { generatePath } from 'react-router-dom';

interface BaseRoutes {
  root: string;
  login: string;
  submoduleList: string;
  projects: string;
  editProject: string;
  employees: string;
  editEmployee: string;
}

const baseRoutes: BaseRoutes = {
  root: '/',
  login: '/login',
  submoduleList: '/submodule-list',
  projects: '/projects',
  editProject: '/projects/:id',
  employees: '/employees',
  editEmployee: '/employees/:id',
};

interface Routes extends Omit<BaseRoutes, 'editProject' | 'editEmployee'> {
  editProject: (id?: string) => string;
  editEmployee: (id?: string) => string;
}

export const routes: Routes = {
  ...baseRoutes,
  editProject: id =>
    id ? generatePath(baseRoutes.editProject, { id }) : baseRoutes.editProject,
  editEmployee: id =>
    id
      ? generatePath(baseRoutes.editEmployee, { id })
      : baseRoutes.editEmployee,
};
</file>

<file path="src/core/theme/index.ts">
export * from './theme-provider.component';
export * from './theme';
</file>

<file path="src/core/theme/theme-provider.component.tsx">
import React from 'react';
import { ThemeProvider, CssBaseline } from '@mui/material';
import { theme } from './theme';

export const ThemeProviderComponent = (props) => {
  const { children } = props;

  return (
    <ThemeProvider theme={theme}>
      <CssBaseline />
      {children}
    </ThemeProvider>
  );
};
</file>

<file path="src/core/theme/theme.ts">
import merge from 'lodash.merge';
import {
  createTheme,
  Theme as DefaultTheme,
  PaletteColor,
} from '@mui/material';

const defaultTheme = createTheme({
  palette: {
    primary: {
      light: '#4a8089',
      main: '#1a535c',
      dark: '#002a33',
    },
    secondary: {
      light: '#fff584',
      main: '#d6c254',
      dark: '#a29223',
    },
    success: {
      main: '#43a047',
    },
    info: {
      main: '#1976d2',
    },
    warning: {
      main: '#ffa000',
    },
  },
  components: {
    MuiTab: {
      styleOverrides: {
        root: {
          color: '#ffffff',
          '&.Mui-selected': {
            color: '#ffffff',
            textDecoration: 'underline',
          },
        },
      },
    },
  },
});

type Theme = DefaultTheme & {
  palette: {
    primary: PaletteColor;
    secondary: PaletteColor;
    success: PaletteColor;
    info: PaletteColor;
    warning: PaletteColor;
    table: {
      row: PaletteColor;
    };
  };
};

export const theme: Theme = merge(defaultTheme, {
  palette: {
    table: {
      row: {
        main: '#ddd',
      },
    },
  },
} as Theme);
</file>

<file path="src/core/i18n.ts">
import { Validators } from '@lemoncode/fonk';
import { matchField } from '@lemoncode/fonk-match-field-validator';

export const setValidatorsMessagesToSpanish = () => {
  Validators.required.setErrorMessage('Debe informar el campo');
  Validators.email.setErrorMessage(
    'La dirección de correo no está bien formada'
  );
  matchField.setErrorMessage('El campo debe coincidir con {{field}}');
};

export const literals = {
  global: {
    copyrightFooter: 'Todos los derechos reservados',
    lemoncode: 'Lemoncode',
    welcome: 'Hola',
  },
  messages: {
    errors: {
      invalidLogin: 'Usuario y/o password no válidos',
    },
  },
  components: {
    actions: {
      save: 'Guardar',
      cancel: 'Cancelar',
      login: 'Iniciar sesión',
      logout: 'Cerrar sesión',
    },
    fields: {
      user: 'Usuario',
      password: 'Contraseña',
      myProfile: 'Mi Perfil',
    },
    date: {
      today: 'Hoy',
    },
  },
} as const;
</file>

<file path="src/layouts/app.layout.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const container = css`
  display: flex;
  flex-direction: column;
  min-height: 100vh;
`;

export const main = css`
  flex: 1;
  padding: ${theme.spacing(0.5)};
`;
</file>

<file path="src/layouts/app.layout.tsx">
import React from 'react';
import { AppBarComponent } from '#common-app/app-bar';
import { FooterComponent } from '#common/components';
import * as classes from './app.layout.styles';

interface Props {
  children: React.ReactNode;
}

export const AppLayout: React.FC<Props> = ({ children }) => (
  <div className={classes.container}>
    <AppBarComponent />
    <main className={classes.main}>{children}</main>
    <FooterComponent />
  </div>
);
</file>

<file path="src/layouts/centered.layout.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const root = css`
  display: grid;
  grid-template-columns: 1fr;
  align-items: center;
  margin-top: 2rem;

  @media (min-width: ${theme.breakpoints.values.sm}px) {
    justify-items: center;
  }
`;
</file>

<file path="src/layouts/centered.layout.tsx">
import React from 'react';
import * as classes from './centered.layout.styles';

interface Props {
  children: React.ReactNode;
}

export const CenteredLayout: React.FC<Props> = (props) => {
  const { children } = props;
  return <div className={classes.root}>{children}</div>;
};
</file>

<file path="src/layouts/index.ts">
export * from './app.layout';
export * from './centered.layout';
</file>

<file path="src/pods/employee/api/employee.api-model.ts">
export interface Employee {
  id: string;
  name: string;
  email: string;
  isActive: boolean;
  temporalPassword?: string;
  projects?: ProjectSummary[];
}

export interface ProjectSummary {
  id: string;
  isAssigned?: boolean;
  projectName: string;
}
</file>

<file path="src/pods/employee/api/employee.api.ts">
import { Employee } from './employee.api-model';
import { mockEmployee } from './employee.mock-data';

export const getEmployeeById = async (id: string): Promise<Employee> => {
  return mockEmployee;
};
</file>

<file path="src/pods/employee/api/employee.mock-data.ts">
import { Employee, ProjectSummary } from './employee.api-model';

const mockProjectSummaryList: ProjectSummary[] = [
  {
    id: '1',
    isAssigned: true,
    projectName: 'Mapfre',
  },
  {
    id: '2',
    isAssigned: false,
    projectName: 'Bankia',
  },
  {
    id: '3',
    isAssigned: false,
    projectName: 'Vacaciones',
  },
  {
    id: '4',
    isAssigned: true,
    projectName: 'Baja',
  },
];

export const mockEmployee: Employee = {
  id: '1',
  name: 'Prueba Nombre',
  email: 'prueba@email.com',
  isActive: true,
  temporalPassword: 'admin',
  projects: mockProjectSummaryList,
};
</file>

<file path="src/pods/employee/api/index.ts">
export * from './employee.api';
</file>

<file path="src/pods/employee/components/data.component.tsx">
import React from 'react';
import { Formik, Form } from 'formik';
import { TextFieldComponent, CheckboxComponent } from '#common/components';
import { produce } from 'immer';
import { CommandFooterComponent } from '../../../common-app/command-footer';
import * as classes from './data.styles';
import { cx } from '@emotion/css';
import { Employee } from '../employee.vm';
import { formValidation, validationSchema } from './data.validations';
import { Validators } from '@lemoncode/fonk';

interface Props {
  employee: Employee;
  className?: string;
  isEditMode: boolean;
  onSave: (employee: Employee) => void;
  onCancel: () => void;
}

export const DataComponent: React.FunctionComponent<Props> = ({
  employee,
  className,
  onSave,
  isEditMode,
  onCancel,
}) => {
  React.useEffect(() => {
    const newValidationSchema = produce(validationSchema, (darft) => {
      darft.field.temporalPassword = isEditMode ? [] : [Validators.required];
    });
    formValidation.updateValidationSchema(newValidationSchema);
  }, [isEditMode]);

  return (
    <Formik
      initialValues={employee}
      enableReinitialize={true}
      onSubmit={onSave}
      validate={formValidation.validateForm}
    >
      {() => (
        <Form className={cx(classes.form({ isEditMode }), className)}>
          <TextFieldComponent
            label="Id"
            name="id"
            className={classes.id}
            slotProps={{
              input: {
                readOnly: true,
              },
            }}
          />
          {!isEditMode && (
            <TextFieldComponent
              label="Clave Temporal"
              name="temporalPassword"
              className={classes.temporalPassword}
            />
          )}
          <TextFieldComponent
            label="Nombre"
            name="name"
            className={classes.name}
          />
          <TextFieldComponent
            label="Email"
            name="email"
            className={classes.email}
          />
          <CheckboxComponent
            name="isActive"
            label="Activo"
            color="primary"
            className={classes.isActive}
          />
          <CommandFooterComponent
            onCancel={onCancel}
            className={classes.commands}
          />
        </Form>
      )}
    </Formik>
  );
};
</file>

<file path="src/pods/employee/components/data.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

interface Props {
  isEditMode: boolean;
}

const getDynamicAreaNames = (props: Props) => {
  return props.isEditMode
    ? `'id employeeName'`
    : `'id temporalPassword' 'employeeName .'`;
};

export const form = (props: Props) => css`
  display: grid;
  grid-template-columns: 1fr;
  grid-template-areas:
    'id'
    ${props.isEditMode ? '' : `'temporalPassword'`}
    'employeeName'
    'email'
    'isActive'
    'commands'
    'commands';
  grid-row-gap: ${theme.spacing(2)};
  grid-column-gap: ${theme.spacing(3)};

  ${theme.breakpoints.up('md')} {
    grid-template-columns: 1fr 1fr;
    grid-template-areas:
      ${getDynamicAreaNames(props)}
      'email .'
      'isActive .'
      'commands commands';
  }
`;

export const id = css`
  grid-area: id;
`;

export const temporalPassword = css`
  grid-area: temporalPassword;
`;

export const name = css`
  grid-area: employeeName;
`;

export const email = css`
  grid-area: email;
`;

export const isActive = css`
  grid-area: isActive;
`;

export const commands = css`
  grid-area: commands;
`;
</file>

<file path="src/pods/employee/components/data.validations.ts">
import { ValidationSchema, Validators } from '@lemoncode/fonk';
import { createFormikValidation } from '@lemoncode/fonk-formik';
import { requiredByField } from '@lemoncode/fonk-required-by-field-validator';

export const validationSchema: ValidationSchema = {
  field: {
    name: [Validators.required],
    email: [
      {
        validator: Validators.required,
      },
      {
        validator: Validators.email,
      },
    ],
    temporalPassword: [
      {
        validator: requiredByField.validator,
        customArgs: {
          field: 'id',
          condition: fieldValue => fieldValue === '',
        },
      },
    ],
  },
};

export const formValidation = createFormikValidation(validationSchema);
</file>

<file path="src/pods/employee/components/index.ts">
export * from './data.component';
export * from './project.component';
export * from './report.component';
</file>

<file path="src/pods/employee/components/project-row.component.tsx">
import React from 'react';
import { Checkbox } from '@mui/material';
import {
  RowRendererProps,
  RowComponent,
  CellComponent,
} from '#common/components';
import { ProjectSummary } from '../employee.vm';

type Props = RowRendererProps<ProjectSummary>;

export const EmployeeRowComponent: React.FunctionComponent<Props> = ({
  row,
}) => {
  return (
    <RowComponent>
      <CellComponent>
        <Checkbox checked={row.isAssigned} color="primary" />
      </CellComponent>
      <CellComponent>{row.projectName}</CellComponent>
    </RowComponent>
  );
};
</file>

<file path="src/pods/employee/components/project.component.tsx">
import React from 'react';
import { TableContainer, RowRendererProps } from '#common/components';
import { ProjectSummary } from '../employee.vm';
import { EmployeeRowComponent } from './project-row.component';
import { CommandFooterComponent } from '#common-app/command-footer';

interface Props {
  projectSummaryList: ProjectSummary[];
  className?: string;
  onCancel: () => void;
}

export const ProjectComponent: React.FunctionComponent<Props> = ({
  projectSummaryList,
  className,
  onCancel,
}) => {
  return (
    <>
      <TableContainer
        columns={['Asignado', 'Nombre y Apellido']}
        rows={projectSummaryList}
        className={className}
        rowRenderer={(rowProps: RowRendererProps<ProjectSummary>) => (
          <EmployeeRowComponent {...rowProps} />
        )}
      />
      <CommandFooterComponent onCancel={onCancel} />
    </>
  );
};
</file>

<file path="src/pods/employee/components/report.component.tsx">
import React from 'react';
import { Formik, Form } from 'formik';
import { SelectComponent } from '#common/components';
import { monthList } from '#common/constants';
import * as classes from './report.styles';
import { CommandFooterComponent } from '#common-app/command-footer';
import { cx } from '@emotion/css';
import { formValidation } from './report.validations';
import { Report } from '../employee.vm';

interface Props {
  report: Report;
  className?: string;
  onCancel: () => void;
  onGenerateExcel: (report: Report) => void;
}

export const ReportComponent: React.FunctionComponent<Props> = ({
  report,
  className,
  onCancel,
  onGenerateExcel,
}) => {
  return (
    <Formik
      initialValues={report}
      enableReinitialize={true}
      onSubmit={onGenerateExcel}
      validate={formValidation.validateForm}
    >
      {() => (
        <Form className={cx(classes.form, className)}>
          <SelectComponent
            name="month"
            label="Mes"
            items={monthList}
            className={classes.month}
          />
          <SelectComponent
            name="year"
            label="Año"
            items={[
              {
                id: '2020',
                name: '2020',
              },
            ]}
            className={classes.year}
          />
          <CommandFooterComponent
            onCancel={onCancel}
            className={classes.commands}
            labels={{ saveButton: 'Generar', cancelButton: 'Cancelar' }}
          />
        </Form>
      )}
    </Formik>
  );
};
</file>

<file path="src/pods/employee/components/report.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const form = css`
  display: grid;
  grid-template-columns: 1fr;
  grid-template-areas: 'month' 'year' 'commands' 'commands';
  grid-column-gap: ${theme.spacing(2)};

  ${theme.breakpoints.up('md')} {
    grid-template-columns: 1fr 1fr;
    grid-template-areas: 'month year' 'commands commands';
  }
`;

export const month = css`
  grid-area: month;
`;

export const year = css`
  grid-area: year;
`;

export const commands = css`
  grid-area: commands;
`;
</file>

<file path="src/pods/employee/components/report.validations.ts">
import { ValidationSchema, Validators } from '@lemoncode/fonk';
import { createFormikValidation } from '@lemoncode/fonk-formik';

const validationSchema: ValidationSchema = {
  field: {
    month: [Validators.required],
    year: [Validators.required],
  },
};

export const formValidation = createFormikValidation(validationSchema);
</file>

<file path="src/pods/employee/employee.component.tsx">
import React from 'react';
import { AppBar } from '@mui/material';
import {
  TabComponent,
  TabListComponent,
  TabPanelComponent,
} from '#common/components';
import { DataComponent, ProjectComponent, ReportComponent } from './components';
import { Employee, Report } from './employee.vm';
import * as classes from './employee.styles';

interface Props {
  employee: Employee;
  isEditMode: boolean;
  report: Report;
  onSave: (employee: Employee) => void;
  onCancel: () => void;
  onGenerateExcel: (report: Report) => void;
}

export const EmployeeComponent: React.FunctionComponent<Props> = ({
  employee,
  isEditMode,
  report,
  onSave,
  onCancel,
  onGenerateExcel,
}) => {
  const [tab, setTab] = React.useState(0);
  return (
    <>
      <AppBar position="static">
        <TabListComponent value={tab} onChange={setTab}>
          <TabComponent label="Datos" />
          <TabComponent label="Proyectos" disabled={!isEditMode} />
          <TabComponent label="Informes" disabled={!isEditMode} />
        </TabListComponent>
      </AppBar>
      <TabPanelComponent value={tab} index={0}>
        <DataComponent
          employee={employee}
          className={classes.root}
          onSave={onSave}
          isEditMode={isEditMode}
          onCancel={onCancel}
        />
      </TabPanelComponent>
      <TabPanelComponent value={tab} index={1}>
        <ProjectComponent
          projectSummaryList={employee.projects}
          className={classes.root}
          onCancel={onCancel}
        />
      </TabPanelComponent>
      <TabPanelComponent value={tab} index={2}>
        <ReportComponent
          report={report}
          className={classes.root}
          onGenerateExcel={onGenerateExcel}
          onCancel={onCancel}
        />
      </TabPanelComponent>
    </>
  );
};
</file>

<file path="src/pods/employee/employee.container.tsx">
import React from 'react';
import { EmployeeComponent } from './employee.component';
import {
  Employee,
  Report,
  createEmptyEmployee,
  createEmptyReport,
} from './employee.vm';
import { useSnackbarContext } from '#common/components';
import { trackPromise } from 'react-promise-tracker';
import { getEmployeeById } from './api';
import { mapEmployeeFromApiToVm } from './employee.mappers';
import { useParams } from 'react-router-dom';
import { isEditModeHelper } from '#common/helpers';

export const EmployeeContainer: React.FunctionComponent = () => {
  const { id } = useParams<{ id: string }>();
  const [employee, setEmployee] = React.useState<Employee>(
    createEmptyEmployee()
  );
  const [isEditMode, setIsEditMode] = React.useState<boolean>(false);
  const [report, setReport] = React.useState<Report>(createEmptyReport());
  const { showMessage } = useSnackbarContext();

  const onLoadEmployee = async () => {
    try {
      const apiEmployee = await trackPromise(getEmployeeById(id));
      const viewModelEmployee = mapEmployeeFromApiToVm(apiEmployee);
      setEmployee(viewModelEmployee);
    } catch (error) {
      error &&
        showMessage('Ha ocurrido un error al cargar el empleado', 'error');
    }
  };

  const handleSave = (employee: Employee) => {
    console.log('Guardado');
  };

  const handleCancel = () => {
    history.back();
  };

  const handleGenerateExcel = (report: Report) => {
    // Pending to create real implementation
    console.log('Excel creado');
  };

  React.useEffect(() => {
    const isEditMode = isEditModeHelper(id);
    setIsEditMode(isEditMode);
    if (isEditMode) {
      onLoadEmployee();
    }
  }, []);

  return (
    <EmployeeComponent
      employee={employee}
      isEditMode={isEditMode}
      report={report}
      onSave={handleSave}
      onCancel={handleCancel}
      onGenerateExcel={handleGenerateExcel}
    />
  );
};
</file>

<file path="src/pods/employee/employee.mappers.spec.ts">
import { mapEmployeeFromApiToVm } from './employee.mappers';
import * as apiModel from './api/employee.api-model';
import * as viewModel from './employee.vm';

describe('./pods/employee/employee.mappers', () => {
  it('should return empty employee when feeding null value', () => {
    // Arrange
    const employee = null;

    // Act
    const result = mapEmployeeFromApiToVm(employee);

    // Assert
    expect(result).toEqual(viewModel.createEmptyEmployee());
  });

  it('should return empty employee when feeding undefined value', () => {
    // Arrange
    const employee = undefined;

    // Act
    const result = mapEmployeeFromApiToVm(employee);

    // Assert
    expect(result).toEqual(viewModel.createEmptyEmployee());
  });

  it('should return expected result but feeding null project list', () => {
    // Arrange
    const employee: apiModel.Employee = {
      id: 'test id',
      name: 'test name',
      email: 'test@email.com',
      isActive: true,
      temporalPassword: 'test password',
      projects: null,
    };

    const expectedResult: viewModel.Employee = {
      id: 'test id',
      name: 'test name',
      email: 'test@email.com',
      isActive: true,
      temporalPassword: 'test password',
      projects: [],
    };

    // Act
    const result = mapEmployeeFromApiToVm(employee);

    // Assert
    expect(result).toEqual(expectedResult);
  });

  it('should return expected result but feeding undefined project list', () => {
    // Arrange
    const employee: apiModel.Employee = {
      id: 'test id',
      name: 'test name',
      email: 'test@email.com',
      isActive: true,
      temporalPassword: 'test password',
      projects: undefined,
    };

    const expectedResult: viewModel.Employee = {
      id: 'test id',
      name: 'test name',
      email: 'test@email.com',
      isActive: true,
      temporalPassword: 'test password',
      projects: [],
    };

    // Act
    const result = mapEmployeeFromApiToVm(employee);

    // Assert
    expect(result).toEqual(expectedResult);
  });

  it('should return expected result feeding correct values', () => {
    // Arrange
    const employee: apiModel.Employee = {
      id: 'test id',
      name: 'test name',
      email: 'test@email.com',
      isActive: true,
      temporalPassword: 'test password',
      projects: [
        {
          id: 'test id',
          projectName: 'test employee name',
          isAssigned: true,
        },
      ],
    };

    const expectedResult: viewModel.Employee = {
      id: 'test id',
      name: 'test name',
      email: 'test@email.com',
      isActive: true,
      temporalPassword: 'test password',
      projects: [
        {
          id: 'test id',
          projectName: 'test employee name',
          isAssigned: true,
        },
      ],
    };

    // Act
    const result = mapEmployeeFromApiToVm(employee);

    // Assert
    expect(result).toEqual(expectedResult);
  });
});
</file>

<file path="src/pods/employee/employee.mappers.ts">
import { mapToCollection } from '#common/mappers';
import * as apiModel from './api/employee.api-model';
import * as viewModel from './employee.vm';

const mapProjectSummaryFromApiToVm = (
  projectSummary: apiModel.ProjectSummary
): viewModel.ProjectSummary => ({
  ...projectSummary,
});

const mapProjectSummaryListFromApiToVm = (
  projectSummary: apiModel.ProjectSummary[]
): viewModel.ProjectSummary[] =>
  mapToCollection(projectSummary, ps => mapProjectSummaryFromApiToVm(ps));

export const mapEmployeeFromApiToVm = (
  employee: apiModel.Employee
): viewModel.Employee => {
  return Boolean(employee)
    ? {
        ...employee,
        projects: mapProjectSummaryListFromApiToVm(employee.projects),
      }
    : viewModel.createEmptyEmployee();
};
</file>

<file path="src/pods/employee/employee.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const root = css`
  margin-top: ${theme.spacing(1.5)};
`;
</file>

<file path="src/pods/employee/employee.vm.ts">
export interface Employee {
  id: string;
  name: string;
  email: string;
  isActive: boolean;
  temporalPassword?: string;
  projects: ProjectSummary[];
}

export interface ProjectSummary {
  id: string;
  isAssigned?: boolean;
  projectName: string;
}

export interface Report {
  month: string;
  year: string;
}

export const createEmptyEmployee = (): Employee => ({
  id: '',
  name: '',
  email: '',
  isActive: false,
  temporalPassword: '',
  projects: [],
});

export const createEmptyReport = (): Report => ({
  month: '',
  year: '',
});
</file>

<file path="src/pods/employee/index.ts">
export * from './employee.container';
</file>

<file path="src/pods/employee-list/api/employee-list.api-model.ts">
export interface Employee {
  id: string;
  isActive: boolean;
  name: string;
  email: string;
  lastDateIncurred: string;
}
</file>

<file path="src/pods/employee-list/api/employee-list.api.ts">
import { Employee } from './employee-list.api-model';
import { mockEmployeeList } from './employee-list.mock-data';

let employeeList = [...mockEmployeeList];

export const getEmployeeList = async (): Promise<Employee[]> => {
  return employeeList;
};

export const deleteEmployee = async (id: string): Promise<boolean> => {
  employeeList = employeeList.filter(e => e.id !== id);
  return true;
};
</file>

<file path="src/pods/employee-list/api/employee-list.mock-data.ts">
import { Employee } from './employee-list.api-model';

export const mockEmployeeList: Employee[] = [
  {
    id: '1',
    isActive: true,
    name: 'Daniel Perez',
    email: 'daniel.perez@empresa.com',
    lastDateIncurred: '02/02/2020',
  },
  {
    id: '2',
    isActive: true,
    name: 'Jose Gomez',
    email: 'jose.gomez@empresa.com',
    lastDateIncurred: '05/02/2020',
  },
  {
    id: '3',
    isActive: false,
    name: 'Manuel Ruiz',
    email: 'manuel.ruiz@empresa.com',
    lastDateIncurred: '06/02/2020',
  },
  {
    id: '4',
    isActive: true,
    name: 'Ramón Gomez',
    email: 'ramon.gomez@empresa.com',
    lastDateIncurred: '02/05/2020',
  },
  {
    id: '5',
    isActive: false,
    name: 'María Lopez',
    email: 'maria.lopez@empresa.com',
    lastDateIncurred: '05/08/2020',
  },
  {
    id: '6',
    isActive: true,
    name: 'Manuel Ortiz',
    email: 'manuel.ortiz@empresa.com',
    lastDateIncurred: '06/06/2020',
  },
  {
    id: '7',
    isActive: false,
    name: 'David Martos',
    email: 'david.martos@empresa.com',
    lastDateIncurred: '14/08/2020',
  },
  {
    id: '8',
    isActive: true,
    name: 'Luz Roca',
    email: 'luz.roca@empresa.com',
    lastDateIncurred: '20/06/2020',
  },
];
</file>

<file path="src/pods/employee-list/api/index.ts">
export * from './employee-list.api';
</file>

<file path="src/pods/employee-list/components/employee-row.component.tsx">
import React from 'react';
import { Checkbox, IconButton } from '@mui/material';
import EditIcon from '@mui/icons-material/Edit';
import DeleteIcon from '@mui/icons-material/Delete';
import {
  RowRendererProps,
  RowComponent,
  CellComponent,
} from '#common/components';
import { Employee } from '../employee-list.vm';

type Props = RowRendererProps<Employee>;

export const EmployeeRowComponent: React.FunctionComponent<Props> = ({
  row,
  onEdit,
  onDelete,
}) => {
  return (
    <RowComponent>
      <CellComponent>
        <Checkbox checked={row.isActive} disabled />
      </CellComponent>
      <CellComponent>{row.id}</CellComponent>
      <CellComponent>{row.name}</CellComponent>
      <CellComponent>{row.email}</CellComponent>
      <CellComponent>
        {row.lastDateIncurred}
        <IconButton onClick={() => onEdit(row.id)}>
          <EditIcon />
        </IconButton>
        <IconButton onClick={() => onDelete(row)}>
          <DeleteIcon />
        </IconButton>
      </CellComponent>
    </RowComponent>
  );
};
</file>

<file path="src/pods/employee-list/components/index.ts">
export * from './employee-row.component';
</file>

<file path="src/pods/employee-list/employee-list.component.tsx">
import React from 'react';
import {
  TableContainer,
  RowRendererProps,
  useSearchBar,
} from '#common/components';
import { Employee } from './employee-list.vm';
import { EmployeeRowComponent } from './components';

interface Props {
  employeeList: Employee[];
  onCreate: () => void;
  onEdit: (id: string) => void;
  onDelete: (id: string) => void;
}

export const EmployeeListComponent: React.FunctionComponent<Props> = ({
  employeeList,
  onCreate,
  onEdit,
  onDelete,
}) => {
  const { filteredList, onSearch, search } = useSearchBar(employeeList, [
    'name',
  ]);

  const contentRender = ({ itemName }) => {
    return (
      <>
        ¿Seguro que quiere borrar a <strong>{itemName}</strong>?
      </>
    );
  };

  return (
    <TableContainer
      columns={['Activo', 'Id', 'Nombre', 'Email', 'Fecha último incurrido']}
      rows={filteredList}
      rowRenderer={(rowProps: RowRendererProps<Employee>) => (
        <EmployeeRowComponent {...rowProps} />
      )}
      onCreate={onCreate}
      onEdit={onEdit}
      onDelete={onDelete}
      labels={{
        searchPlaceholder: 'Buscar empleado',
        createButton: 'Nuevo empleado',
        deleteTitle: 'Eliminar Empleado',
        deleteContent: props => contentRender(props),
        closeButton: 'Cancelar',
        acceptButton: 'Aceptar',
      }}
      enableSearch={true}
      search={search}
      onSearch={onSearch}
      enablePagination={true}
      pageSize={5}
    />
  );
};
</file>

<file path="src/pods/employee-list/employee-list.container.tsx">
import React from 'react';
import { useNavigate } from 'react-router-dom';
import { EmployeeListComponent } from './employee-list.component';
import { getEmployeeList, deleteEmployee } from './api';
import { Employee } from './employee-list.vm';
import { useSnackbarContext } from '#common/components';
import { trackPromise } from 'react-promise-tracker';
import { mapEmployeeListFromApiToVm } from './employee-list.mappers';
import { routes } from '#core/router';
const editEmployeeId = '0';

export const EmployeeListContainer: React.FunctionComponent = () => {
  const [employees, setEmployees] = React.useState<Employee[]>([]);
  const { showMessage } = useSnackbarContext();
  const navigate = useNavigate();

  const onLoadEmployeeList = async () => {
    try {
      const apiEmployeeList = await trackPromise(getEmployeeList());
      const viewModelEmloyeeList = mapEmployeeListFromApiToVm(apiEmployeeList);
      setEmployees(viewModelEmloyeeList);
    } catch (error) {
      error &&
        showMessage('Ha ocurrido un error al cargar los empleados', 'error');
    }
  };

  const handleCreate = () => {
    navigate(routes.editEmployee(editEmployeeId));
  };

  const handleEdit = (id: string) => {
    navigate(routes.editEmployee(id));
  };

  const handleDelete = async (id: string) => {
    const errorMessage = 'Error al eliminar un empleado';
    try {
      const isDeleted = await trackPromise(deleteEmployee(id));
      isDeleted ? onLoadEmployeeList() : showMessage(errorMessage, 'error');
    } catch (error) {
      error && showMessage(errorMessage, 'error');
    }
  };

  React.useEffect(() => {
    onLoadEmployeeList();
  }, []);

  return (
    <EmployeeListComponent
      employeeList={employees}
      onCreate={handleCreate}
      onEdit={handleEdit}
      onDelete={handleDelete}
    />
  );
};
</file>

<file path="src/pods/employee-list/employee-list.mappers.spec.ts">
import * as apiModel from './api/employee-list.api-model';
import * as viewModel from './employee-list.vm';
import { mapEmployeeListFromApiToVm } from './employee-list.mappers';

describe('./pods/employee-list', () => {
  it('should return empty array when feeding undefined employee list', () => {
    // Arrange
    const employeeList = undefined;

    // Act
    const result = mapEmployeeListFromApiToVm(employeeList);

    // Assert
    expect(result).toEqual([]);
  });

  it('should return empty array when feeding null employee list', () => {
    // Arrange
    const employeeList = null;

    // Act
    const result = mapEmployeeListFromApiToVm(employeeList);

    // Assert
    expect(result).toEqual([]);
  });

  it('should return empty array when feeding empty array employee list', () => {
    // Arrange
    const employeeList = [];

    // Act
    const result = mapEmployeeListFromApiToVm(employeeList);

    // Assert
    expect(result).toEqual([]);
  });

  it('should return one item with values when passing one item with values', () => {
    // Arrange
    const employeeList: apiModel.Employee[] = [
      {
        id: 'test id',
        isActive: true,
        name: 'test name',
        email: 'test@email.com',
        lastDateIncurred: '02/02/2020',
      },
    ];

    const expectedResult: viewModel.Employee[] = [
      {
        id: 'test id',
        isActive: true,
        name: 'test name',
        email: 'test@email.com',
        lastDateIncurred: '02/02/2020',
      },
    ];

    // Act
    const result = mapEmployeeListFromApiToVm(employeeList);

    // Assert
    expect(result).toEqual(expectedResult);
  });

  it('should return two item with values when passing two item with values', () => {
    // Arrange
    const employeeList: apiModel.Employee[] = [
      {
        id: 'test id 1',
        isActive: true,
        name: 'test name 1',
        email: 'test@email.com',
        lastDateIncurred: '02/02/2020',
      },
      {
        id: 'test id 2',
        isActive: true,
        name: 'test name 2',
        email: 'test@email.com',
        lastDateIncurred: '02/02/2020',
      },
    ];

    const expectedResult: viewModel.Employee[] = [
      {
        id: 'test id 1',
        isActive: true,
        name: 'test name 1',
        email: 'test@email.com',
        lastDateIncurred: '02/02/2020',
      },
      {
        id: 'test id 2',
        isActive: true,
        name: 'test name 2',
        email: 'test@email.com',
        lastDateIncurred: '02/02/2020',
      },
    ];

    // Act
    const result = mapEmployeeListFromApiToVm(employeeList);

    // Assert
    expect(result).toEqual(expectedResult);
  });
});
</file>

<file path="src/pods/employee-list/employee-list.mappers.ts">
import { mapToCollection } from '#common/mappers';
import * as apiModel from './api/employee-list.api-model';
import * as viewModel from './employee-list.vm';

const mapEmployeeFromApiToVm = (
  employee: apiModel.Employee
): viewModel.Employee => ({
  ...employee,
});

export const mapEmployeeListFromApiToVm = (
  employeeList: apiModel.Employee[]
): viewModel.Employee[] =>
  mapToCollection(employeeList, e => mapEmployeeFromApiToVm(e));
</file>

<file path="src/pods/employee-list/employee-list.vm.ts">
export interface Employee {
  id: string;
  isActive: boolean;
  name: string;
  email: string;
  lastDateIncurred: string;
}
</file>

<file path="src/pods/employee-list/index.ts">
export * from './employee-list.container';
</file>

<file path="src/pods/login/components/container.component.tsx">
import React from 'react';
import { Container, Avatar, Typography } from '@mui/material';
import LockOutlinedIcon from '@mui/icons-material/LockOutlined';
import * as classes from './container.styles';

interface Props {
  title: string;
  children: React.ReactNode;
}

export const ContainerComponent: React.FC<Props> = ({ children, title }) => (
  <Container component="main" maxWidth="xs">
    <div className={classes.paper}>
      <Avatar className={classes.avatar}>
        <LockOutlinedIcon />
      </Avatar>
      <Typography component="h1" variant="h5">
        {title}
      </Typography>
      {children}
    </div>
  </Container>
);
</file>

<file path="src/pods/login/components/container.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const paper = css`
  margin-top: ${theme.spacing(8)};
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export const avatar = css`
  margin: ${theme.spacing(1)};
  background-color: ${theme.palette.secondary.main};
`;
</file>

<file path="src/pods/login/components/index.ts">
export * from './container.component';
export * from './login-form.component';
</file>

<file path="src/pods/login/components/login-form.component.tsx">
import React from 'react';
import { Formik, Form } from 'formik';
import { Button } from '@mui/material';
import { TextFieldComponent } from '#common/components';
import { Login, createEmptyLogin } from '../login.vm';
import { formValidation } from '../login.validation';
import * as classes from './login-form.styles';
import { literals } from '#core/i18n';

interface Props {
  onLogin: (login: Login) => void;
}

export const LoginFormComponent: React.FunctionComponent<Props> = (props) => {
  const { onLogin } = props;
  return (
    <Formik
      onSubmit={onLogin}
      initialValues={createEmptyLogin()}
      validate={formValidation.validateForm}
    >
      {() => (
        <Form className={classes.root}>
          <TextFieldComponent
            name="user"
            label={`${literals.components.fields.user} *`}
            variant="outlined"
          />
          <TextFieldComponent
            name="password"
            label={`${literals.components.fields.password} *`}
            type="password"
            variant="outlined"
          />
          <Button
            type="submit"
            variant="contained"
            color="primary"
            className={classes.submit}
          >
            Login
          </Button>
        </Form>
      )}
    </Formik>
  );
};
</file>

<file path="src/pods/login/components/login-form.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const root = css`
  display: flex;
  flex-direction: column;
  align-items: center;
  & > :nth-child(n) {
    margin-top: 1rem;
  }

  @media (min-width: ${theme.breakpoints.values.sm}px) {
    min-width: 400px;
  }
`;

export const submit = css`
  margin: ${theme.spacing(3, 0, 2)};
`;
</file>

<file path="src/pods/login/index.ts">
export * from './login.container';
</file>

<file path="src/pods/login/login.api.ts">
export const isValidLogin = (
  user: string,
  password: string
): Promise<boolean> =>
  new Promise(resolve => {
    setTimeout(() => {
      resolve(user === 'admin' && password === 'test');
    }, 1000);
  });
</file>

<file path="src/pods/login/login.component.tsx">
import React from 'react';
import { ContainerComponent, LoginFormComponent } from './components';
import { Login } from './login.vm';

interface Props {
  onLogin: (login: Login) => void;
}

export const LoginComponent: React.FunctionComponent<Props> = ({ onLogin }) => {
  return (
    <ContainerComponent title="Login">
      <LoginFormComponent onLogin={onLogin} />
    </ContainerComponent>
  );
};
</file>

<file path="src/pods/login/login.container.tsx">
import React from 'react';
import { trackPromise } from 'react-promise-tracker';
import { useNavigate } from 'react-router-dom';
import { routes } from '#core/router';
import { literals } from '#core/i18n';
import { AuthContext } from '#common-app/auth';
import { useSnackbarContext } from '#common/components';
import { isValidLogin } from './login.api';
import { LoginComponent } from './login.component';
import { Login } from './login.vm';
import { mapLoginResponseToUserSession } from './login.mapper';

export const LoginContainer: React.FunctionComponent = () => {
  const { setUserSession } = React.useContext(AuthContext);
  const { showMessage } = useSnackbarContext();
  const navigate = useNavigate();

  // TODO: Pending to implement with real data
  const loginSucceeded = (isValid: boolean): void => {
    if (isValid) {
      const userSession = mapLoginResponseToUserSession();
      userSession.userName = 'Admin';
      setUserSession(userSession);
      navigate(routes.submoduleList);
    } else {
      showMessage(literals.messages.errors.invalidLogin, 'error');
    }
  };

  const handleLogin = (login: Login) => {
    trackPromise(isValidLogin(login.user, login.password)).then(loginSucceeded);
  };

  return <LoginComponent onLogin={handleLogin} />;
};
</file>

<file path="src/pods/login/login.mapper.ts">
import { UserSession, createEmptyUserSession } from '#common-app/auth';

export const mapLoginResponseToUserSession = (): UserSession =>
  createEmptyUserSession();
</file>

<file path="src/pods/login/login.validation.ts">
import { ValidationSchema, Validators } from '@lemoncode/fonk';
import { createFormikValidation } from '@lemoncode/fonk-formik';

const validationSchema: ValidationSchema = {
  field: {
    user: [Validators.required],
    password: [Validators.required],
  },
};

export const formValidation = createFormikValidation(validationSchema);
</file>

<file path="src/pods/login/login.vm.ts">
export interface Login {
  user: string;
  password: string;
}

export const createEmptyLogin = (): Login => ({
  user: '',
  password: '',
});
</file>

<file path="src/pods/project/api/employee.api.ts">
import { Project } from './project.api-model';
import { mockProject } from './project.mock-data';

export const getProjectById = async (id: string): Promise<Project> => {
  return mockProject;
};
</file>

<file path="src/pods/project/api/index.ts">
export * from './employee.api';
</file>

<file path="src/pods/project/api/project.api-model.ts">
export interface Project {
  id: string;
  name: string;
  externalId?: string;
  comments?: string;
  isActive: boolean;
  employees: EmployeeSummary[];
}

export interface EmployeeSummary {
  id: string;
  isAssigned?: boolean;
  employeeName: string;
}
</file>

<file path="src/pods/project/api/project.mock-data.ts">
import { Project, EmployeeSummary } from './project.api-model';

const mockEmployeeSummaryList: EmployeeSummary[] = [
  {
    id: '1',
    employeeName: 'Daniel Perez',
    isAssigned: true,
  },
  {
    id: '2',
    employeeName: 'Jose Sanchez',
    isAssigned: false,
  },
  {
    id: '3',
    employeeName: 'Javier Benitez',
    isAssigned: false,
  },
  {
    id: '4',
    employeeName: 'María Peña',
    isAssigned: true,
  },
];

export const mockProject: Project = {
  id: '1',
  name: 'Nombre',
  isActive: true,
  comments: 'Comentario',
  externalId: '1234',
  employees: mockEmployeeSummaryList,
};
</file>

<file path="src/pods/project/components/data.component.tsx">
import React from 'react';
import { Formik, Form } from 'formik';
import { TextFieldComponent, CheckboxComponent } from '#common/components';
import { CommandFooterComponent } from '../../../common-app/command-footer';
import { Project } from '../project.vm';

interface Props {
  project: Project;
  onSave: (project: Project) => void;
  onCancel: () => void;
  className: string;
}

export const DataComponent: React.FunctionComponent<Props> = ({
  project,
  onSave,
  onCancel,
  className,
}) => {
  return (
    <Formik initialValues={project} enableReinitialize={true} onSubmit={onSave}>
      {() => (
        <Form className={className}>
          <TextFieldComponent
            label="Id"
            name="id"
            slotProps={{
              input: {
                readOnly: true,
              },
            }}
          />
          <TextFieldComponent label="Nombre" name="name" />
          <TextFieldComponent label="Id Externo" name="externalId" />
          <TextFieldComponent label="Comentarios" name="comments" multiline />
          <CheckboxComponent label="Activo" name="isActive" color="primary" />
          <CommandFooterComponent onCancel={onCancel} />
        </Form>
      )}
    </Formik>
  );
};
</file>

<file path="src/pods/project/components/employee-row.component.tsx">
import React from 'react';
import { Checkbox } from '@mui/material';
import {
  RowRendererProps,
  RowComponent,
  CellComponent,
} from '#common/components';
import { EmployeeSummary } from '../project.vm';

type Props = RowRendererProps<EmployeeSummary>;

export const ProjectRowComponent: React.FunctionComponent<Props> = ({
  row,
}) => {
  return (
    <RowComponent>
      <CellComponent>
        <Checkbox checked={row.isAssigned} color="primary" />
      </CellComponent>
      <CellComponent>{row.employeeName}</CellComponent>
    </RowComponent>
  );
};
</file>

<file path="src/pods/project/components/employee.component.tsx">
import React from 'react';
import { TableContainer, RowRendererProps } from '#common/components';
import { EmployeeSummary } from '../project.vm';
import { CommandFooterComponent } from '#common-app/command-footer';
import { ProjectRowComponent } from './employee-row.component';

interface Props {
  employeeSummaryList: EmployeeSummary[];
  onCancel: () => void;
  className: string;
}

export const EmployeeComponent: React.FunctionComponent<Props> = ({
  employeeSummaryList,
  onCancel,
  className,
}) => {
  return (
    <>
      <TableContainer
        columns={['Asignado', 'Empleado']}
        rows={employeeSummaryList}
        className={className}
        rowRenderer={(rowProps: RowRendererProps<EmployeeSummary>) => (
          <ProjectRowComponent {...rowProps} />
        )}
      />
      <CommandFooterComponent onCancel={onCancel} />
    </>
  );
};
</file>

<file path="src/pods/project/components/index.ts">
export * from './data.component';
export * from './employee.component';
export * from './report.component';
</file>

<file path="src/pods/project/components/report.component.tsx">
import React from 'react';
import { Formik, Form } from 'formik';
import { SelectComponent } from '#common/components';
import { monthList } from '#common/constants';
import { CommandFooterComponent } from '#common-app/command-footer';
import { cx } from '@emotion/css';
import * as classes from './report.styles';

interface Props {
  onCancel: () => void;
  className: string;
}

export const ReportComponent: React.FunctionComponent<Props> = ({
  onCancel,
  className,
}) => {
  return (
    <Formik initialValues={{}} enableReinitialize={true} onSubmit={console.log}>
      {() => (
        <Form className={cx(classes.form, className)}>
          <SelectComponent
            name="month"
            label="Mes"
            items={monthList}
            disabled
            className={classes.month}
          />
          <SelectComponent
            name="year"
            label="Año"
            items={[
              {
                id: '2020',
                name: '2020',
              },
            ]}
            disabled
            className={classes.year}
          />
          <CommandFooterComponent
            onCancel={onCancel}
            labels={{ saveButton: 'Generar', cancelButton: 'Cancelar' }}
            className={classes.commands}
          />
        </Form>
      )}
    </Formik>
  );
};
</file>

<file path="src/pods/project/components/report.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const form = css`
  display: grid;
  grid-template-columns: 1fr;
  grid-template-areas: 'month' 'year' 'commands' 'commands';
  grid-column-gap: ${theme.spacing(2)};

  @media (min-width: ${theme.breakpoints.values.md}px) {
    grid-template-columns: 1fr 1fr;
    grid-template-areas: 'month year' 'commands commands';
  }
`;

export const month = css`
  grid-area: month;
`;

export const year = css`
  grid-area: year;
`;

export const commands = css`
  grid-area: commands;
`;
</file>

<file path="src/pods/project/index.ts">
export * from './project.container';
</file>

<file path="src/pods/project/project.component.tsx">
import React from 'react';
import { AppBar } from '@mui/material';
import {
  TabComponent,
  TabListComponent,
  TabPanelComponent,
} from '#common/components';
import {
  DataComponent,
  EmployeeComponent,
  ReportComponent,
} from './components';
import { Project } from './project.vm';
import * as classes from './project.styles';

interface Props {
  isEditMode: boolean;
  project: Project;
  onSave: (project: Project) => void;
  onCancel: () => void;
}

export const ProjectComponent: React.FunctionComponent<Props> = ({
  isEditMode,
  project,
  onSave,
  onCancel,
}) => {
  const [tab, setTab] = React.useState(0);
  return (
    <>
      <AppBar position="static">
        <TabListComponent value={tab} onChange={setTab}>
          <TabComponent label="Datos" />
          <TabComponent label="Empleados" disabled={!isEditMode} />
          <TabComponent label="Informes" disabled={!isEditMode} />
        </TabListComponent>
      </AppBar>
      <TabPanelComponent value={tab} index={0}>
        <DataComponent
          project={project}
          onCancel={onCancel}
          onSave={onSave}
          className={classes.root}
        />
      </TabPanelComponent>
      <TabPanelComponent value={tab} index={1}>
        <EmployeeComponent
          employeeSummaryList={project.employees}
          onCancel={onCancel}
          className={classes.root}
        />
      </TabPanelComponent>
      <TabPanelComponent value={tab} index={2}>
        <ReportComponent onCancel={onCancel} className={classes.root} />
      </TabPanelComponent>
    </>
  );
};
</file>

<file path="src/pods/project/project.container.tsx">
import React from 'react';
import { ProjectComponent } from './project.component';
import { useParams } from 'react-router-dom';
import { useSnackbarContext } from '#common/components';
import { getProjectById } from './api';
import { trackPromise } from 'react-promise-tracker';
import { mapProjectFromApiToVm } from './project.mapper';
import { Project, createEmptyProject } from './project.vm';
import { isEditModeHelper } from '#common/helpers';

export const ProjectContainer: React.FunctionComponent = () => {
  const { id } = useParams<{ id: string }>();
  const [project, setProject] = React.useState<Project>(createEmptyProject());
  const [isEditMode, setIsEditMode] = React.useState<boolean>(false);
  const { showMessage } = useSnackbarContext();

  const onLoadProject = async () => {
    try {
      const apiProject = await trackPromise(getProjectById(id));
      const viewModelProject = mapProjectFromApiToVm(apiProject);
      setProject(viewModelProject);
    } catch (error) {
      error &&
        showMessage('Ha ocurrido un error al cargar el proyecto', 'error');
    }
  };

  const handleSave = (Project: Project) => {
    console.log('Guardado');
  };

  const handleCancel = () => {
    history.back();
  };

  React.useEffect(() => {
    const isEditMode = isEditModeHelper(id);
    setIsEditMode(isEditMode);
    if (isEditMode) {
      onLoadProject();
    }
  }, []);

  return (
    <ProjectComponent
      isEditMode={isEditMode}
      project={project}
      onSave={handleSave}
      onCancel={handleCancel}
    />
  );
};
</file>

<file path="src/pods/project/project.mapper.ts">
import { mapToCollection } from '#common/mappers';
import * as apiModel from './api/project.api-model';
import * as viewModel from './project.vm';

const mapEmployeeSummaryFromApiToVm = (
  employeeSummary: apiModel.EmployeeSummary
): viewModel.EmployeeSummary => ({
  ...employeeSummary,
});

const mapEmployeeSummaryListFromApiToVm = (
  employeeSummary: apiModel.EmployeeSummary[]
): viewModel.EmployeeSummary[] =>
  mapToCollection(employeeSummary, es => mapEmployeeSummaryFromApiToVm(es));

export const mapProjectFromApiToVm = (
  project: apiModel.Project
): viewModel.Project => {
  return Boolean(project)
    ? {
        ...project,
        employees: mapEmployeeSummaryListFromApiToVm(project.employees),
      }
    : viewModel.createEmptyProject();
};
</file>

<file path="src/pods/project/project.styles.ts">
import { css } from '@emotion/css';
import { theme } from '#core/theme';

export const root = css`
  margin-top: ${theme.spacing(1.5)};
`;
</file>

<file path="src/pods/project/project.vm.ts">
export interface Project {
  id: string;
  name: string;
  externalId?: string;
  comments?: string;
  isActive: boolean;
  employees: EmployeeSummary[];
}

export interface EmployeeSummary {
  id: string;
  isAssigned?: boolean;
  employeeName: string;
}

export const createEmptyProject = (): Project => ({
  id: '',
  name: '',
  externalId: '',
  comments: '',
  isActive: false,
  employees: [],
});
</file>

<file path="src/pods/project-list/api/index.ts">
export * from './project-list.api';
</file>

<file path="src/pods/project-list/api/project-list.api-model.ts">
export interface Project {
  id: string;
  isActive: boolean;
  code: string;
  name: string;
  lastDateIncurred: string;
  creationDate: string;
}
</file>

<file path="src/pods/project-list/api/project-list.api.ts">
import { Project } from './project-list.api-model';
import { mockProjectList } from './project-list.mock-data';

let projectList = [...mockProjectList];

export const getProjectList = async (): Promise<Project[]> => {
  return projectList;
};

export const deleteProject = async (id: string): Promise<boolean> => {
  projectList = projectList.filter(p => p.id !== id);
  return true;
};
</file>

<file path="src/pods/project-list/api/project-list.mock-data.ts">
import { Project } from './project-list.api-model';

export const mockProjectList: Project[] = [
  {
    id: '1',
    isActive: true,
    code: '23212',
    name: 'Bankia',
    lastDateIncurred: '02/02/2020',
    creationDate: '01/08/2018',
  },
  {
    id: '2',
    isActive: true,
    code: '4323',
    name: 'Mapfre',
    lastDateIncurred: '05/02/2020',
    creationDate: '01/04/2018',
  },
  {
    id: '3',
    isActive: true,
    code: '002',
    name: 'Vacaciones',
    lastDateIncurred: '05/02/2020',
    creationDate: '01/04/2018',
  },
  {
    id: '4',
    isActive: true,
    code: '003',
    name: 'Baja Médica',
    lastDateIncurred: '05/03/2018',
    creationDate: '01/05/2019',
  },
  {
    id: '5',
    isActive: false,
    code: '2586',
    name: 'Proyecto interno',
    lastDateIncurred: '05/08/2020',
    creationDate: '01/10/2018',
  },
  {
    id: '6',
    isActive: false,
    code: '3025',
    name: 'BBVA',
    lastDateIncurred: '06/05/2020',
    creationDate: '01/03/2019',
  },
  {
    id: '7',
    isActive: false,
    code: '8563',
    name: 'Baja Médica',
    lastDateIncurred: '02/08/2018',
    creationDate: '01/11/2020',
  },
  {
    id: '8',
    isActive: true,
    code: '4125',
    name: 'Microsoft España',
    lastDateIncurred: '11/10/2018',
    creationDate: '01/07/2020',
  },
];
</file>

<file path="src/pods/project-list/components/index.ts">
export * from './project-row.component';
</file>

<file path="src/pods/project-list/components/project-row.component.tsx">
import React from 'react';
import { Checkbox, IconButton } from '@mui/material';
import EditIcon from '@mui/icons-material/Edit';
import DeleteIcon from '@mui/icons-material/Delete';
import {
  RowRendererProps,
  RowComponent,
  CellComponent,
} from '#common/components';
import { Project } from '../project-list.vm';

type Props = RowRendererProps<Project>;

export const ProjectRowComponent: React.FunctionComponent<Props> = ({
  row,
  onEdit,
  onDelete,
}) => {
  return (
    <RowComponent>
      <CellComponent>
        <Checkbox checked={row.isActive} disabled />
      </CellComponent>
      <CellComponent>{row.code}</CellComponent>
      <CellComponent>{row.name}</CellComponent>
      <CellComponent>{row.lastDateIncurred}</CellComponent>
      <CellComponent>
        {row.creationDate}
        <IconButton onClick={() => onEdit(row.id)}>
          <EditIcon />
        </IconButton>
        <IconButton onClick={() => onDelete(row)}>
          <DeleteIcon />
        </IconButton>
      </CellComponent>
    </RowComponent>
  );
};
</file>

<file path="src/pods/project-list/index.ts">
export * from './project-list.container';
</file>

<file path="src/pods/project-list/project-list.component.tsx">
import React from 'react';
import {
  TableContainer,
  RowRendererProps,
  useSearchBar,
} from '#common/components';
import { Project } from './project-list.vm';
import { ProjectRowComponent } from './components';

interface Props {
  projectList: Project[];
  onCreate: () => void;
  onEdit: (id: string) => void;
  onDelete: (id: string) => void;
}

export const ProjectListComponent: React.FunctionComponent<Props> = ({
  projectList,
  onCreate,
  onEdit,
  onDelete,
}) => {
  const { filteredList, onSearch, search } = useSearchBar(projectList, [
    'name',
  ]);

  const contentRender = ({ itemName }) => {
    return (
      <>
        ¿Seguro que quiere borrar a <strong>{itemName}</strong>?
      </>
    );
  };

  return (
    <TableContainer
      columns={[
        'Activo',
        'Código',
        'Proyecto',
        'Fecha Ultimo incurrido',
        'Fecha creación',
      ]}
      rows={filteredList}
      rowRenderer={(rowProps: RowRendererProps<Project>) => (
        <ProjectRowComponent {...rowProps} />
      )}
      onCreate={onCreate}
      onEdit={onEdit}
      onDelete={onDelete}
      labels={{
        searchPlaceholder: 'Buscar proyecto',
        createButton: 'Nuevo proyecto',
        deleteTitle: 'Eliminar Proyecto',
        deleteContent: props => contentRender(props),
        closeButton: 'Cancelar',
        acceptButton: 'Aceptar',
      }}
      enableSearch={true}
      search={search}
      onSearch={onSearch}
      enablePagination={true}
      pageSize={5}
    />
  );
};
</file>

<file path="src/pods/project-list/project-list.container.tsx">
import React from 'react';
import { useNavigate } from 'react-router-dom';
import { ProjectListComponent } from './project-list.component';
import { getProjectList, deleteProject } from './api';
import { Project } from './project-list.vm';
import { useSnackbarContext } from '#common/components';
import { trackPromise } from 'react-promise-tracker';
import { mapProjectListFromApiToVm } from './project-list.mappers';
import { routes } from '#core/router';
const editProjectId = '0';

export const ProjectListContainer: React.FunctionComponent = () => {
  const [projects, setProjects] = React.useState<Project[]>([]);
  const { showMessage } = useSnackbarContext();
  const navigate = useNavigate();

  const onLoadProjectList = async () => {
    try {
      const apiProjectList = await trackPromise(getProjectList());
      const viewModelProjectList = mapProjectListFromApiToVm(apiProjectList);
      setProjects(viewModelProjectList);
    } catch (error) {
      error &&
        showMessage('Ha ocurrido un error al cargar los proyectos', 'error');
    }
  };

  const handleCreate = () => {
    navigate(routes.editProject(editProjectId));
  };

  const handleEdit = (id: string) => {
    navigate(routes.editProject(id));
  };

  const handleDelete = async (id: string) => {
    const errorMessage = 'Error al eliminar un proyecto';
    try {
      const isDeleted = await trackPromise(deleteProject(id));
      isDeleted ? onLoadProjectList() : showMessage(errorMessage, 'error');
    } catch (error) {
      error && showMessage(errorMessage, 'error');
    }
  };

  React.useEffect(() => {
    onLoadProjectList();
  }, []);

  return (
    <ProjectListComponent
      projectList={projects}
      onCreate={handleCreate}
      onEdit={handleEdit}
      onDelete={handleDelete}
    />
  );
};
</file>

<file path="src/pods/project-list/project-list.mappers.spec.ts">
import * as apiModel from './api/project-list.api-model';
import * as viewModel from './project-list.vm';
import { mapProjectListFromApiToVm } from './project-list.mappers';

describe('./pods/project-list', () => {
  it('should return empty array when feeding undefined project list', () => {
    // Arrange
    const projectList = undefined;

    // Act
    const result = mapProjectListFromApiToVm(projectList);

    // Assert
    expect(result).toEqual([]);
  });

  it('should return empty array when feeding null project list', () => {
    // Arrange
    const projectList = null;

    // Act
    const result = mapProjectListFromApiToVm(projectList);

    // Assert
    expect(result).toEqual([]);
  });

  it('should return empty array when feeding empty array project list', () => {
    // Arrange
    const projectList = [];

    // Act
    const result = mapProjectListFromApiToVm(projectList);

    // Assert
    expect(result).toEqual([]);
  });

  it('should return one item with values when passing one item with values', () => {
    // Arrange
    const projectList: apiModel.Project[] = [
      {
        id: 'test id',
        isActive: true,
        code: 'test code',
        name: 'test project name',
        lastDateIncurred: '02/02/2020',
        creationDate: '05/11/2019',
      },
    ];

    const expectedResult: viewModel.Project[] = [
      {
        id: 'test id',
        isActive: true,
        code: 'test code',
        name: 'test project name',
        lastDateIncurred: '02/02/2020',
        creationDate: '05/11/2019',
      },
    ];

    // Act
    const result = mapProjectListFromApiToVm(projectList);

    // Assert
    expect(result).toEqual(expectedResult);
  });

  it('should return two item with values when passing two item with values', () => {
    // Arrange
    const projectList: apiModel.Project[] = [
      {
        id: 'test id 1',
        isActive: true,
        code: 'test code',
        name: 'test project name 1',
        lastDateIncurred: '02/02/2020',
        creationDate: '05/11/2019',
      },
      {
        id: 'test id 2',
        isActive: false,
        code: 'test code',
        name: 'test project name 2',
        lastDateIncurred: '02/11/2020',
        creationDate: '05/05/2019',
      },
    ];

    const expectedResult: viewModel.Project[] = [
      {
        id: 'test id 1',
        isActive: true,
        code: 'test code',
        name: 'test project name 1',
        lastDateIncurred: '02/02/2020',
        creationDate: '05/11/2019',
      },
      {
        id: 'test id 2',
        isActive: false,
        code: 'test code',
        name: 'test project name 2',
        lastDateIncurred: '02/11/2020',
        creationDate: '05/05/2019',
      },
    ];

    // Act
    const result = mapProjectListFromApiToVm(projectList);

    // Assert
    expect(result).toEqual(expectedResult);
  });
});
</file>

<file path="src/pods/project-list/project-list.mappers.ts">
import { mapToCollection } from '#common/mappers';
import * as apiModel from './api/project-list.api-model';
import * as viewModel from './project-list.vm';

const mapProjectFromApiToVm = (
  project: apiModel.Project
): viewModel.Project => ({
  ...project,
});

export const mapProjectListFromApiToVm = (
  projectList: apiModel.Project[]
): viewModel.Project[] =>
  mapToCollection(projectList, p => mapProjectFromApiToVm(p));
</file>

<file path="src/pods/project-list/project-list.vm.ts">
export interface Project {
  id: string;
  isActive: boolean;
  code: string;
  name: string;
  lastDateIncurred: string;
  creationDate: string;
}
</file>

<file path="src/pods/submodule-list/index.ts">
export * from './submodule-list.container';
</file>

<file path="src/pods/submodule-list/submodule-list.component.tsx">
import React from 'react';
import { DashboardComponent, DashboardItemProps } from '#common/components';

interface Props {
  items: DashboardItemProps[];
}

export const SumoduleListComponent: React.FunctionComponent<Props> = props => {
  const { items } = props;
  return <DashboardComponent items={items} />;
};
</file>

<file path="src/pods/submodule-list/submodule-list.container.tsx">
import React from 'react';
import AccountBalanceIcon from '@mui/icons-material/AccountBalance';
import GroupIcon from '@mui/icons-material/Group';
import { SumoduleListComponent } from './submodule-list.component';
import { DashboardItemProps } from '#common/components';
import { routes } from '#core/router';

export const SubmoduleListContainer: React.FunctionComponent = () => {
  const items: DashboardItemProps[] = React.useMemo(
    (): DashboardItemProps[] => [
      {
        title: 'Proyectos',
        linkTo: routes.projects,
        icon: AccountBalanceIcon,
      },
      {
        title: 'Empleados',
        linkTo: routes.employees,
        icon: GroupIcon,
      },
    ],
    []
  );

  return <SumoduleListComponent items={items} />;
};
</file>

<file path="src/scenes/employee-list.scene.tsx">
import React from 'react';
import { AppLayout } from '#layouts';
import { EmployeeListContainer } from '#pods/employee-list';

export const EmployeeListScene: React.FunctionComponent = () => {
  return (
    <AppLayout>
      <EmployeeListContainer />
    </AppLayout>
  );
};
</file>

<file path="src/scenes/employee.scene.tsx">
import React from 'react';
import { AppLayout } from '#layouts';
import { EmployeeContainer } from '#pods/employee';

export const EmployeeScene: React.FunctionComponent = () => {
  return (
    <AppLayout>
      <EmployeeContainer />
    </AppLayout>
  );
};
</file>

<file path="src/scenes/index.ts">
export * from './login.scene';
export * from './submodule-list.scene';
export * from './project-list.scene';
export * from './employee-list.scene';
export * from './project.scene';
export * from './employee.scene';
</file>

<file path="src/scenes/login.scene.tsx">
import React from 'react';
import { CenteredLayout } from '#layouts';
import { LoginContainer } from '#pods/login';

export const LoginScene: React.FunctionComponent = () => {
  return (
    <CenteredLayout>
      <LoginContainer />
    </CenteredLayout>
  );
};
</file>

<file path="src/scenes/project-list.scene.tsx">
import React from 'react';
import { AppLayout } from '#layouts';
import { ProjectListContainer } from '#pods/project-list';

export const ProjectListScene: React.FunctionComponent = () => {
  return (
    <AppLayout>
      <ProjectListContainer />
    </AppLayout>
  );
};
</file>

<file path="src/scenes/project.scene.tsx">
import React from 'react';
import { AppLayout } from '#layouts';
import { ProjectContainer } from '#pods/project';

export const ProjectScene: React.FunctionComponent = () => {
  return (
    <AppLayout>
      <ProjectContainer />
    </AppLayout>
  );
};
</file>

<file path="src/scenes/submodule-list.scene.tsx">
import React from 'react';
import { AppLayout } from '#layouts';
import { SubmoduleListContainer } from '#pods/submodule-list';

export const SubmoduleListScene: React.FC = () => {
  return (
    <AppLayout>
      <SubmoduleListContainer />
    </AppLayout>
  );
};
</file>

<file path="src/app.tsx">
import React from 'react';
import { StyledEngineProvider } from '@mui/material';
import { setValidatorsMessagesToSpanish } from '#core/i18n';
import { RouterComponent } from '#core/router';
import { ThemeProviderComponent } from '#core/theme';
import { AuthProvider } from '#common-app/auth';
import {
  SnackbarComponent,
  SnackbarProvider,
  SpinnerComponent,
} from '#common/components';

setValidatorsMessagesToSpanish();

const App: React.FunctionComponent = () => {
  return (
    <StyledEngineProvider injectFirst>
      <ThemeProviderComponent>
        <AuthProvider>
          <SnackbarProvider>
            <SpinnerComponent />
            <RouterComponent />
            <SnackbarComponent />
          </SnackbarProvider>
        </AuthProvider>
      </ThemeProviderComponent>
    </StyledEngineProvider>
  );
};

export default App;
</file>

<file path="src/index.tsx">
import React from 'react';
import { createRoot } from 'react-dom/client';
import App from './app';

const root = createRoot(document.getElementById('root'));
root.render(<App />);
</file>

</files>
